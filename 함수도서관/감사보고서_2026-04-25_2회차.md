# 중복 함수 감사 보고서 — 2026-04-25 (2회차)

> **스코프**: 1회차 (`감사보고서_2026-04-25.md`) 이후 추가된 영역 + 1회차 보류·반려 재검토 + S3 작업으로 신설된 코드의 신규 중복.
>
> **목적**: 1회차 §1 후보 100% 처리 (✅ 19 + ⚠️ 5 도입 안 함) 완료. 잔존 중복 패턴 발굴 → §1.10+ 신규 후보 등록.
>
> **이 보고서의 위상**: 제안서. 코드 변경 없음. 후속 라운드에서 후보별 분리 PR.

---

## 1. 1회차 처리 결과 요약

| 카테고리 | 1회차 후보 | 처리 결과 |
|----------|----------|----------|
| BE §1 (B1~B12) | 12 후보 | ✅ 11 승격 (B10 emit_audit 만 ⚠️ 도입 안 함, BE-G4 결정) |
| FE §1 (F1~F8) | 8 후보 | ✅ 7 승격 (formatRelativeShort 만 ⚠️ 도입 안 함, FE-G1 결정) |
| 누계 | **20 후보** | **✅ 18 / ⚠️ 2 / 미처리 0** |

추가로 호출지 마이그레이션 약 200+ 사이트 + 인프라 (R9 ContextVar) + 결정 D1~D7 처리.

---

## 2. 2회차 발굴 방법

1. 1회차 §3.2 보류·반려 카테고리 재점검 (시간 경과 + S3 작업 후 의미 변화).
2. S3 Phase 2 작업으로 추가된 패턴 (folders/collections/tags/scope_profile) grep.
3. 보안 가드 / 응답 envelope / SQL builder 등 1회차 미커버 영역 발굴.

---

## 3. 백엔드 2회차 신규 후보

### 3.1 확정 후보

| # | 후보 | 제안 배치 | 실측 빈도 | 1회당 크기 | 비고 |
|---|------|----------|-----------|------------|------|
| **B-N1** | `require_actor_id(actor, *, label)` | `app.utils.actor` 확장 | folders/collections `_require_actor` 본문 100% 동일 (메시지만 도메인) | 4줄 × 2 | B6 normalize_display_name 와 동일 패턴 (label 인자 통합) |
| **B-N2** | `require_admin(actor)` | `app.utils.actor` 확장 | proposal_queue + scope_profiles 본문 100% 동일 (`if not actor.is_authenticated or actor.role not in _ADMIN_ROLES: raise ApiPermissionDeniedError("관리자 권한이 필요합니다.")`) | 3줄 × 2 | vectorization / admin_extraction_results 의 다른 admin 가드 함수와는 시그니처 다름 (별도 검토) |
| **B-N3** | `ADMIN_ROLES = frozenset(["ORG_ADMIN", "SUPER_ADMIN"])` | `app.api.auth.constants` (신설) | proposal_queue + scope_profiles + 다른 곳에 `_ADMIN_ROLES` 모듈 상수 중복 | 1줄 × 3+ | B-N2 의 의존 — 같이 도입 |
| **B-N4** | `not_found_resource(resource_type, resource_id, *, lang="ko")` | `app.utils.http_errors` 확장 | `f"{Resource} '{id}' not found"` 영문 24 사이트 (Document 15 / Version 3 / Folder 3 / Collection 2 / 기타 1) | 1줄 | BE-G3 not_found 위 wrapper. 한국어 메시지 사전 통일 가능 (`f"{label}을(를) 찾을 수 없습니다"`) |

### 3.2 보류·반려 (재검토 후에도 보류)

- **`success_response()` wrap 213회** — 이미 단일 함수 (`app.api.responses`) 가 표준. 추가 wrapper 의문.
- **WHERE 동적 조건 SQL builder** — admin.py 7회 + repos 3회. 1회차 보류 사유 (도메인별 의미 다름) 유효. 신규 코드 직접 작성 권장.
- **RETURNING + commit + fetchone 트랜잭션** 98회 — fetch_one_as 가 commit/RETURNING 책임 안 짐. 호출자가 트랜잭션 흐름 명시 유지.
- **162 emitter 호출자 wrap** — R9 ContextVar 도입으로 자동 trace_id 첨부 가능. 명시 wrapper 가치 ↓.

### 3.3 1회차 ⚠️ 결정 재검토 결과 (변경 없음)

- **B10 `emit_audit` (도입 안 함)** — 162 호출자 컨텍스트 다양성 + AuditEmitter 가 이미 풍부한 시그니처. 결정 유지.

---

## 4. 프론트엔드 2회차 신규 후보

### 4.1 확정 후보

| # | 후보 | 제안 배치 | 실측 빈도 | 1회당 크기 | 비고 |
|---|------|----------|-----------|------------|------|
| **F-N1** | `FORM_ERROR_CLASS` 토큰 (또는 `<FormErrorMessage>` 컴포넌트) | `lib/styles/tokens.ts` 확장 또는 `components/form/FormErrorMessage.tsx` 신설 | `text-xs text-red-600 mt-1` 변형 5+ 패턴 (`mt-1` / `mt-1.5` / `mb-3` / role="alert" / break-words / animate-in). 폼 에러 표시용 inline `<p>` | 1줄 (className) | R3 ALERT_ERROR_COMPACT 와 다른 결 — 인라인 폼 에러 (작고, p 태그). 토큰화 + role="alert" 일관 |

### 4.2 보류·반려

- **`<EmptyState title=...>` 26 사이트** — 이미 단일 컴포넌트. 도메인별 메시지 사전화는 i18n 도입 시 한 번에. 본 라운드 X.
- **`useMutationWithToast` 38회** — 이미 단일 helper. 추가 도입 X.
- **`useAuthz` + `can()` 31회** — 이미 단일 helper. 추가 도입 X.
- **`queryKey` 180회 / `invalidateQueries` 79회** — 1회차 §2.1 결정 유지 (queryKey 도메인별 다양성으로 통합 X).
- **AdminGuard 컴포넌트** — `SidebarUserPanel.tsx` 의 `hasMinimumRole("ORG_ADMIN")` 1 사이트만. 신설 가치 ↓.

---

## 5. 스팟 체크 원본 카운트 (2026-04-25 2회차 grep)

| 패턴 | BE/FE | 카운트 | 비고 |
|------|-------|--------|------|
| `def _require_actor` | BE | 2 (folders + collections) | B-N1 |
| `def _require_admin` (본문 동일 케이스) | BE | 2 (proposal_queue + scope_profiles) | B-N2 |
| `_ADMIN_ROLES =` 모듈 상수 | BE | 3+ | B-N3 |
| `raise ApiNotFoundError(f"X '{id}' not found")` 영문 패턴 | BE | 24 | B-N4 |
| `success_response(` | BE | 213 | 보류 |
| `audit_emitter.emit(` | BE | 69 | R9 자동 첨부 후 우선순위 ↓ |
| `<EmptyState` | FE | 26 | 이미 컴포넌트 |
| `useMutationWithToast` | FE | 38 | 이미 helper |
| `text-xs text-red-` 변형 className | FE | 45 (5+ 변형) | F-N1 |
| `queryKey: [` | FE | 180 | 보류 |
| `useAuthz` / `can(` | FE | 31 | 이미 helper |

---

## 6. 후속 턴 권고

5 신규 후보 (B-N1~B-N4 + F-N1) 우선순위:

1. **B-N1 + B-N2 + B-N3 묶음 (BE-G6 권한 가드)** — `app.utils.actor` 확장. 4 helper 동시 도입 + 4 사이트 마이그레이션 + 테스트. **가장 높은 ROI** (보안 가치 + 코드 간결).
2. **B-N4 `not_found_resource`** — BE-G3 not_found 위 wrapper. 24 사이트 마이그레이션. 도서관 §1.5 확장.
3. **F-N1 `FORM_ERROR_CLASS`** — lib/styles/tokens.ts 에 `FORM_ERROR_INLINE` 토큰 추가. 5+ 변형 사이트 점진 마이그레이션 (R3 와 같은 결).

각 PR 은 1회차와 동일한 6 단계 산출물 (유틸 / 테스트 / 호출지 / 검증 / 도서관 ✅ / closure 메모리).

---

## 7. 본 보고서 산출물

- `docs/함수도서관/감사보고서_2026-04-25_2회차.md` (본 문서, 신규)
- `docs/함수도서관/backend.md` §1.10+ 에 B-N1~B-N4 등록 (후속)
- `docs/함수도서관/frontend.md` §1.9 에 F-N1 등록 (후속)

코드 변경 없음. 1회차와 동일한 정책: **감사 + 문서만**, 구현은 후속 라운드.
