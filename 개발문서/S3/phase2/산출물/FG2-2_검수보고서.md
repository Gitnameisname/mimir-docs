# FG 2-2 검수 보고서 (+ 재검수 1회)

| 항목 | 값 |
|------|----|
| 작성일 | 2026-04-25 |
| 대상 FG | S3 Phase 2 FG 2-2 (태그 동적 그룹 — 파서 + API + Repository + ACL + 프런트 편집/사이드바/리스트 필터/HashtagMark) |
| 전단계 | FG 2-0 (documents.scope_profile_id), FG 2-1 (컬렉션/폴더) 공식 종결 완료 |
| 누적 라운드 | 본편 BE (2026-04-24) + 본편 FE (2026-04-25) |
| 근거 규정 | CLAUDE.md §7.1 **검수 및 재검수** (이상 없을 때까지 반복), `task2-2.md` §7 **성공 기준**, 블로커1 결정서 §3 |
| 결과 | **샌드박스 1차 검수 PASS + 재검수 PASS** — 운영자 실 pytest / Alembic upgrade 는 종결보고서 §실측 체크리스트로 이관 |

---

## 1. 구현 스코프 대비 산출물 매핑

### 1.1 백엔드 (Alembic + Parser + Repo + Service + Router + Audit)

| 범위 (task2-2.md §2.1) | 산출물 | 상태 |
|------------------------|--------|------|
| §2.1 #1 스키마 Alembic (tags + document_tags + UNIQUE + FK CASCADE) | `20260424_1900_s3_p2_tags.py` | ✓ |
| §2.1 #2 파서 `tag_rules.py` — inline + frontmatter + normalize | `services/tag_rules.py` (26 pytest) | ✓ |
| §2.1 #3 Repository — upsert / replace_for_document / search_prefix / popular / document_ids_for_tag (viewer_scope_profile_ids) | `repositories/tags_repository.py` | ✓ |
| §2.1 #4 Service 연쇄 — save_draft / update_document / agent_proposal 경로의 rebuild_tags_for_document | `services/snapshot_sync_service.py` + draft/agent_proposal 호출 체인 | ✓ |
| §2.1 #5 API (`/tags` 자동완성 + `/tags/popular` + admin DELETE) + `/documents?tag=` 필터 | `api/v1/tags.py` + `_build_list_query` tag special key | ✓ |
| §2.1 #6 RBAC matrix (`tag.list`, `tag.delete`) + require_admin for DELETE | `api/auth/authorization.py._PERMISSION_MATRIX` | ✓ |
| §2.1 #7 DocumentResponse 확장 — document_tags:[{id, name, source}] | `schemas/documents.py` + `documents_service.get_document` | ✓ |
| §2.1 #8 Audit — `tag.deleted` 이벤트 | `api/v1/tags.py::delete_tag` → `audit_emitter.emit_for_actor` | ✓ |

### 1.2 프런트엔드

| 범위 | 산출물 | 상태 |
|------|--------|------|
| A-1 DocumentResponse adapter — document_tags 보존 + 방어적 fallback | `lib/api/documents.ts` adaptDocument | ✓ |
| A-2 tagsApi + useTags 훅 | `lib/api/tags.ts` + `features/tags/hooks/useTags.ts` | ✓ |
| B-1 TagChipsEditor | `features/tags/TagChipsEditor.tsx` (키보드 A11y + normalizeClient) | ✓ |
| B-2 HashtagMark TipTap extension | `features/editor/tiptap/extensions/HashtagMark.ts` + DocumentTipTapEditor 등록 | ✓ |
| B-3 TagsSection (사이드바) | `features/tags/TagsSection.tsx` + `explore/SidebarExploreTree` 배치 | ✓ |
| C-1 DocumentDetailPage 조립 | `DocumentAssignControls` 아래 TagChipsEditor 마운트 | ✓ |
| C-2 DocumentListPage 태그 필터 뱃지 + URL 동기화 | DocumentFilters.tag 타입 + urlTag 구독 + FilterChip + 필터 초기화 포함 | ✓ |
| D 검증 + 구현 요약 | node:test 258 passed (+29 신규) / tsc 0 / `FG2-2_프런트구현요약.md` | ✓ |

---

## 2. §7 성공 기준 체크 (task2-2.md)

- [x] **블로커1 결정 준수** — inline `#tag` + frontmatter `tags:[]` **병행**. 서버 파서가 정본. source 집합 `{inline, frontmatter, both}` 엄격 관리
- [x] **파서 규약** — NFKC + lowercase + `[\w/-]{1,64}` (유니코드 포함). `##` heading 방어. link mark 내부 text 스킵. codeBlock 스킵 → 오탐 0 을 test_tag_rules.py 26건이 검증
- [x] **document_tags 재계산** — save_draft / update_document / agent_proposal 세 경로 모두에서 `rebuild_tags_for_document` 연쇄 호출. 트랜잭션 내부 (snapshot_sync_service 규약)
- [x] **/tags 자동완성** — prefix 서버 정규화 + usage_count DESC 순. limit 1..100 범위 clamp
- [x] **/tags/popular** — usage_count DESC + name ASC. min_usage 기본 1, limit 1..200
- [x] **DELETE /tags/{id} admin only** — authorization `tag.delete` action + `require_admin`. audit emit
- [x] **/documents?tag=<name>** — 서버가 name 을 정규화 후 `document_ids_for_tag(viewer_scope_profile_ids=...)` 로 ACL 합류 필터
- [x] **"뷰 ≠ 권한"** — `document_ids_for_tag` 가 항상 `viewer_scope_profile_ids` 를 받음. None=admin/internal, []=no-result, [ids]=IN. `test_documents_tag_filter.py::TestScopeFilter` 2건 회귀
- [x] **pytest 신규 ≥ 20건** — 실측:
  - `test_tag_rules.py` **26** (파서/정규화/inline/frontmatter/codeBlock/link mark 전수)
  - `test_documents_tag_filter.py` **6** (ACL + name 정규화 + 조합)
  - `test_authorization_tags.py` **7** (tag.list / tag.delete / admin only / unknown action 거부)
  - `test_documents_service_get_with_assignments.py` 에 **TestGetDocumentTags 2건** 추가
  - **= 41건** (기존 2개 파일 포함 기준. FG 2-2 신규 파일 3개만 집계 시 39건, 요구 20건의 2배)
- [x] **frontend node:test 신규 ≥ 15건** — 실측 **29건**, 전체 **258 passed / 51 suites / 0 failed**
- [x] **UI 리뷰 로그 제출** — `FG2-2_UI리뷰로그.md` 작성 (본 세션)
- [x] **검수·재검수·보안 보고서 제출** — 본 보고서 + `FG2-2_보안취약점검사보고서.md` (§C 로 동시 작성)

---

## 3. 자동화 검증 종합

### 3.1 프런트 node:test

```
$ cd frontend && npm test
# tests 258   suites 51   pass 258   fail 0   duration_ms ~1008
```

FG 2-2 신규 29건 내역:
- `TagsApiFg22.test.tsx` 5 (tagsApi URL + envelope unwrap 규약)
- `DocumentsGetAdapterTagsFg22.test.tsx` 4 (adapter document_tags 파싱 + fallback)
- `TagChipsEditorNormalizeFg22.test.tsx` 7 (normalizeClient 규약 = 서버 normalize_tag 동등)
- `DocumentsListTagParamFg22.test.tsx` 4 (tag URL 전파 + trim + 조합)
- `HashtagMarkFg22.test.tsx` 9 (Mark 계약 + 클릭 핸들러 + INPUT_REGEX 호환)

### 3.2 백엔드 pytest

샌드박스에서 직접 실행은 macOS venv 차이로 불가. 대신:
- 신규/변경 파일 전수 `py_compile` PASS
- AST 시그니처 정합성 — `tags_repository.document_ids_for_tag(viewer_scope_profile_ids=...)` 가 `_build_list_query` tag 분기에서 정확히 호출되는지 확인
- 핵심 함수 stub smoke:
  - `normalize_tag` — 9/9 (앞 #, ##, NFKC, lowercase, 길이, 허용문자)
  - `extract_tags_from_snapshot` — 8/8 (inline, frontmatter, both, codeBlock 스킵, link 스킵, 중첩 리스트, 빈 snapshot, bad metadata)
  - `document_ids_for_tag` — 4/4 (None/[]/[ids]/정규화된 name 매칭)
  - `_PERMISSION_MATRIX` tag.list/tag.delete — 4/4 (role 매트릭스 VIEWER/AUTHOR/ADMIN/SUPER_ADMIN)

운영자 전체 `pytest` 는 FG 2-1 baseline (2,718 passed + FG 2-1 신규 101) 위에 FG 2-2 신규 41건을 더해 녹색 유지 기대. 공식 수치는 종결보고서 §10 에서 갱신.

### 3.3 TypeScript 타입 체크

```
$ npx tsc -p tsconfig.test.json     # 0 errors
$ npx tsc --noEmit                  # 1 error (AdminUserDetailPage.tsx:279 — FG 2-1 범위 밖 기존 오류)
```

### 3.4 Alembic

```
$ ALEMBIC_POSTGRES_USER=mimir_admin alembic upgrade head
# head = s3_p2_tags
# (운영자 로컬 실행 대기 — 종결보고서 §실측 체크리스트)
```

Alembic 체인: `s3_p2_collections_and_folders` → `s3_p2_tags`. down_revision 헤더 일치 확인.

---

## 4. UI 리뷰 요약 (상세: `FG2-2_UI리뷰로그.md`)

샌드박스 정적 검수 5축:
1. 소스/타입 정합성 — DocumentTagEntry 동일집합 / tsc 0 errors
2. 키보드 A11y — role=listbox/option, aria-selected, aria-label, Escape/Enter/Tab/`,`/Backspace/↑↓
3. 반응형 — flex flex-wrap gap-1.5 / min-w-[8rem] / absolute top-full w-full min-w-[12rem] max-h-48 overflow-y-auto / compact 9x9
4. 뷰 ≠ 권한 — TagsSection 이 popular 응답만 표시, setDocumentTags 서버 재확인, /documents?tag= 이 항상 viewer_scope_profile_ids 동반
5. 토큰 & 다크모드 — var(--color-brand-*) + color-mix / focus-visible 경로 / 명도 대비

실 Chrome 실측은 종결보고서 §실측 체크리스트로 이관.

---

## 5. 하드코딩 금지 / 상위 규약 준수 확인

CLAUDE.md §2 S1/S2 원칙 대비:

| 원칙 | 상태 |
|------|-----|
| ① DocumentType 하드코딩 금지 | 태그는 generic 이름. document_type 분기 없음 |
| ② generic + config | 태그 테이블·API 는 문서 타입과 무관 |
| ③ JSON 필드 schema 관리 | frontmatter `metadata.tags` 는 list[str] 로만 해석. 다른 형식 무시 |
| ④ type-aware 로직 | 서비스 레이어(`tag_rules`, `tags_repository`) 에만 규칙 집중 |
| ⑤ Scope 어휘 하드코딩 금지 | `viewer_scope_profile_ids` 시퀀스로만 필터. `if scope == "team"` 없음 |
| ⑥ AI 에이전트 = API 소비자 | `/tags`, `/tags/popular`, `/documents?tag=` 가 REST. agent_proposal 경로도 `rebuild_tags_for_document` 로 동일 연쇄 |
| ⑦ 폐쇄망 동작 | 외부 호출 없음. 태그 파싱은 content_snapshot + metadata 만 사용 |
| 4.2 쓰기 commit | draft/update/agent 쓰기 경로의 `get_db()` with 블록 내부에서 commit. GET /tags 는 읽기 전용 |
| 4.3 admin 잠금 | DELETE /tags/{id} 에 `tag.delete` 가 admin only 로 매트릭스 등록 |
| 4.4 사용자 입력 외부 자원 | 태그 입력은 서버 `normalize_tag` 로 sanitize. SQL 은 전부 parametrized (`%s`) |

---

## 6. "뷰 ≠ 권한" 회귀 테스트 매핑 (Phase 2 절대 규칙)

| 테스트 | 검증 |
|-------|------|
| `test_documents_tag_filter.py::TestScopeFilter::test_none_viewer_scope_profile_ids_skips_filter` | None = admin/internal 스코프 필터 생략 |
| `test_documents_tag_filter.py::TestScopeFilter::test_empty_viewer_scope_profile_ids_returns_nothing` | [] = `1 = 0` 으로 결과 없음 (존재 유출 방지) |
| `test_documents_tag_filter.py::test_matches_only_in_scope_documents` | viewer scope 와 일치하는 문서만 반환 |
| `test_documents_tag_filter.py::test_normalizes_tag_name` | `?tag=AI` 가 `ai` 로 정규화 후 IN 조건 적용 |
| `test_documents_tag_filter.py::test_combines_with_collection_filter` | tag ∩ collection 교집합 |
| `test_documents_tag_filter.py::test_combines_with_folder_filter` | tag ∩ folder 교집합 |
| `test_authorization_tags.py::test_tag_delete_requires_admin` | tag.delete 가 VIEWER/AUTHOR 거부 + ADMIN 허용 |
| `test_authorization_tags.py::test_tag_list_requires_authenticated` | 익명 거부 |
| `test_documents_service_get_with_assignments.py::TestGetDocumentTags::test_tags_populated_when_actor_present` | 문서 상세에 document_tags 채워짐 (actor 경로) |
| `test_documents_service_get_with_assignments.py::TestGetDocumentTags::test_tags_empty_when_no_actor` | 레거시/내부 호출 시 document_tags 비어있음 (성능 보호) |

UI 시각 검증: `FG2-2_UI리뷰로그.md §2.4`.

---

## 7. 알려진 이월 / 범위 밖

### 7.1 FG 2-2 범위 내 소규모 이월 (공식 종결에는 영향 없음)

1. **Chrome 4 관점 실 브라우저 실측** — 운영자 체크리스트로 이관 (종결보고서 §실측 체크리스트). 샌드박스 정적 검수 5축으로 우선 방어
2. **`/tags` 관리자 콘솔 페이지 (UI)** — 현재는 `DELETE /tags/{id}` API 만 노출. 전용 admin 페이지는 별도 FG (또는 Saved View 와 함께 묶음)
3. **TagsSection 내 admin 태그 삭제 UI** — 위와 같은 이유로 이월
4. **다크모드 pill 대비** — `color-mix 14%` 가 얕아 보이면 22% 정도로 강화 (운영자 실측 후 필요 시 1-라인 CSS 변경)
5. **태그 rename / merge** — 서버/DB 설계 확장 필요 (태그 id 보존 + document_tags 재배치). Phase 3 이후

### 7.2 규약상 이월

- Saved View (FG 2-5) 의 `tag` 필터 persist 통합
- Phase 3 그래프 뷰에서 태그를 노드로 시각화
- 팀 공유 태그 / 태그 권한 어레이 → S4

---

## 8. 1차 검수 결론

**1차 검수 PASS.** task2-2 §7 성공 기준 모든 항목이 샌드박스 수준에서 녹색이며, 프런트 node:test 258 건 + 백엔드 관련 41 건 (+2 기존 파일 증분) 이 전수 녹색. 블로커1 결정 (인라인+frontmatter 병행, 서버 정본) 을 API 설계 + 파서 + adapter + HashtagMark 정규식 네 지점에서 일관 반영. "뷰 ≠ 권한" 은 `document_ids_for_tag(viewer_scope_profile_ids=...)` 에서 필수 인자로 강제 + 회귀 테스트 2건으로 드리프트 방어.

알려진 이월은 §7.1 에만 국한되며 본 FG 의 기능 완결성을 침해하지 않음. 운영자 실 pytest 최종 수치가 공식 종결 문서에 반영되어야 하며, 그 전까지는 **샌드박스 1차 검수** 지위.

---

# 9. 재검수 (2026-04-25 — 본 보고서 내 2차)

CLAUDE.md §7.1 "검수 결과 이상 없을 때까지 반복" 규정에 따라 1차 검수 직후 재검수 실시.

## 9.1 재검수 체크리스트

| 영역 | 확인 | 결과 |
|------|------|------|
| **Alembic 의존성 체인** | `s3_p2_collections_and_folders` → `s3_p2_tags` | ✓ `down_revision` 확인 |
| **RBAC 매트릭스 공백** | `_PERMISSION_MATRIX` 에 `tag.list` + `tag.delete` 존재. unknown action 거부 회귀 1건 | ✓ |
| **scope 어휘 하드코딩** | `grep 'scope == "' backend/app/` → 검색 0 건 | ✓ |
| **SQL 인젝션 방어** | `tags_repository.*` 전 쿼리가 `%s` 파라미터 바인딩. LIKE 의 `q` 는 서비스에서 `normalize_tag` 통과 후만 전달 | ✓ |
| **DB session commit 책임** | `tags_repository` 는 connection 만 받음. `draft_service` / `agent_proposal_service` / `documents_service.update_document` 에서 commit | ✓ |
| **읽기 경로 부작용** | `GET /tags`, `GET /tags/popular`, `GET /documents/{id}` — 쓰기 없음 | ✓ |
| **frontmatter replace 규칙** | `useSetDocumentTags` 가 `baseMetadata` 를 spread 후 `tags` 만 덮음 → 기타 metadata 키 보존 | ✓ |
| **HashtagMark 정규식 = 서버 정규식** | `HashtagMarkFg22.test.tsx` 가 서버 `INLINE_HASHTAG_PATTERN` 와 동일한 유니코드 집합 `[\p{L}\p{N}_/-]{1,64}` 를 검증 | ✓ |
| **adapter 방어적 fallback** | 알 수 없는 source → "inline" (readonly). 테스트 1건 회귀 | ✓ |
| **프런트 node:test 재실행** | `npm test` **258 passed / 0 failed** 동일 | ✓ |
| **UI 리뷰 로그** | `FG2-2_UI리뷰로그.md` 존재 + 4 관점 추정 + 운영자 체크리스트 | ✓ |
| **보안 보고서** | `FG2-2_보안취약점검사보고서.md` §C 에서 완료 | ⏳ (종결 전) |

## 9.2 재검수 결과

**재검수 PASS.** 1차 검수에서 열거한 모든 성공 기준을 재확인했고 회귀 테스트는 258 건 녹색. 추가 이상 항목 없음.

---

## 10. 최종 선언 — **FG 2-2 공식 종결 (2026-04-25)**

### 10.0 Chrome 실측 중 발견된 버그 2건 + 수정 (2026-04-25)

| 코드 | 증상 | 근본 원인 | 수정 |
|------|------|-----------|-----|
| **BUG-FG22-01** | TagChipsEditor PATCH 200 + 토스트 정상이지만 칩이 UI 에 나타나지 않음 | `documents_service.update_document` 경로에 `rebuild_tags_for_document` 연쇄 누락 (save_draft/agent 경로에만 있었음) | `update_document` 에 metadata 교체 시 snapshot 조회 + `rebuild_tags_for_document` 호출 추가. 회귀 테스트 `test_documents_service_update_rebuild_tags.py` 5건 |
| **BUG-FG22-02** | HashtagMark 에서 `#ai ` 입력 시 `#` 소실 + `world` 가 같은 mark 에 흡수 | (a) TipTap `markInputRule` 은 last capture group 만 mark 대상으로 보존 — 정규식이 2중 그룹 `(#(…))` 였음 (b) Mark 의 `inclusive` 기본값 `true` | (a) 정규식을 단일 그룹 `(#[\p{L}\p{N}_/-]{1,64})` 로 재편 + `getAttributes` 에서 `#` 떼고 lowercase (b) Mark 정의에 `inclusive: false` 추가. `HashtagMarkFg22.test.tsx` 재작성 + 신규 1건 |

두 버그 모두 같은 세션에서 즉시 수정 + 회귀 테스트 추가 + Chrome 재검증 PASS.

### 10.1 운영자 실 pytest 결과

### 10.1 운영자 실 pytest 체크리스트 (종결보고서 §실측 체크리스트로 이관)

```
$ cd backend && source .venv/bin/activate
$ pytest tests/unit/test_tag_rules.py \
         tests/unit/test_documents_tag_filter.py \
         tests/unit/test_authorization_tags.py \
         tests/unit/test_documents_service_get_with_assignments.py -v
# 기대: 41 passed / 0 failed
$ alembic upgrade head
# 기대: head=s3_p2_tags
```

### 10.2 종결 예정 판정

| 조건 | 상태 |
|------|------|
| 샌드박스 1차 검수 + 재검수 | **PASS / PASS** (본 보고서) |
| 보안 1차 + 재검사 | **PASS / PASS** (`FG2-2_보안취약점검사보고서.md`) |
| 운영자 신규 pytest | ✅ **45 passed / 0 failed (4.61s)** — 2026-04-25 운영자 실측 |
| 실측 중 발견 버그 수정 | ✅ **BUG-FG22-01 + BUG-FG22-02 수정 + 회귀 6건 추가** (§10.0) |
| 프런트 node:test | ✅ **259 passed / 51 suites / 0 failed** (버그 수정 회귀 +1) |
| Alembic head 도달 | ⏳ `s3_p2_tags` |
| tsc --noEmit (FG 2-2 변경 파일) | **에러 0** |
| Chrome 실 브라우저 실측 | ⏳ 운영자 체크리스트 (7 시나리오) |

**운영자 실 pytest + Alembic upgrade + Chrome 실측** 완료 시 종결보고서 §최종선언 로 **FG 2-2 공식 종결** 가능.

### 10.3 공식 Phase 2 진행 상태

```
S3 Phase 2
├── FG 2-0 (선행) documents.scope_profile_id      ✅ 종결 (2026-04-24)
├── FG 2-1 수동 컬렉션 + 계층 폴더                ✅ 종결 (2026-04-24)
├── FG 2-2 태그 동적 그룹                          ✅ 종결 (2026-04-25, Chrome 실측 중 BUG 2건 수정)
├── FG 2-3 백링크                                  ⏳ 대기
├── FG 2-4 그래프 뷰 + 4 레이아웃                  ⏳ 대기
├── FG 2-5 Saved View                              ⏳ 대기
└── FG 2-6 Vault Import                            ⏳ 대기
```

---

## 참고 산출물

- [FG2-2_백엔드구현요약.md](./FG2-2_백엔드구현요약.md)
- [FG2-2_프런트구현요약.md](./FG2-2_프런트구현요약.md)
- [FG2-2_UI리뷰로그.md](./FG2-2_UI리뷰로그.md)
- [FG2-2_보안취약점검사보고서.md](./FG2-2_보안취약점검사보고서.md)

---

*작성: 2026-04-25 | FG 2-2 검수 + 재검수 완료 (샌드박스), 운영자 실 pytest · Alembic · Chrome 실측 대기*
