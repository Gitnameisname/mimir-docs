# FG 6-4 검수보고서 — Admin API organization_id 격리

**작성일**: 2026-05-18
**Phase**: S3 / Phase 6 / FG 6-4
**Handoff Level**: extended (보안 P0 — 권한 경계)

---

## 1. 검수 범위

- `backend/app/utils/admin_org_guard.py` — 신설.
- `backend/app/api/v1/scope_profiles.py` — 10 엔드포인트 (scope-profile 5 + agent 5).
- `backend/app/api/v1/admin.py` — 4 엔드포인트 (user org-roles 2 + organization 2).
- 회귀:
  - `tests/unit/test_admin_org_guard_fg64.py` (8건 — guard 단위 분기).
  - `tests/unit/test_admin_org_isolation_routes_fg64.py` (5건 — Codex P2-1 시정,
    route-level ASGI stack 검증. 4 endpoint group 각각의 cross-org reject + SUPER_ADMIN
    cross-org audit emit. Codex 3차 보강).
- 점검표: `FG6-4_Admin엔드포인트_점검표.md` (별 산출물 — 검증 방식 3축 결합 명시).

## 2. R-O4 (Admin 격리) 점검

| 항목 | 결과 |
|---|---|
| SUPER_ADMIN 만 횡단 허용 | ✅ G2 |
| ORG_ADMIN 본인 조직 통과 | ✅ G3 |
| ORG_ADMIN 다른 조직 거부 (403) | ✅ G4 |
| target_org_id None + non-SUPER_ADMIN 거부 | ✅ G5 |
| 미인증 거부 | ✅ G6 |
| 횡단 시 `admin.cross_org_access` audit emit | ✅ G2 (mock 검증) |

## 3. 회귀 결과 (총 13건 — Codex 3차 P2-1 잔존 보강 후)

```
$ .venv/bin/python -m pytest \
    tests/unit/test_admin_org_guard_fg64.py \
    tests/unit/test_admin_org_isolation_routes_fg64.py
13 passed
```

| 파일 | 건수 | 책임 |
|---|---|---|
| `test_admin_org_guard_fg64.py` | 8 | `ensure_actor_can_access_org` 분기 (G1~G8) |
| `test_admin_org_isolation_routes_fg64.py` | 5 | route-level ASGI stack — 4 endpoint group 각각의 cross-org reject (agent / scope-profile / user-org-role / organization) + SUPER_ADMIN cross-org audit emit (agent). Codex 2차 P2-1 신설 + 3차 보강. |

## 4. 엔드포인트별 통합 위치 (Phase 6 §3 표 14개 일치)

`FG6-4_Admin엔드포인트_점검표.md` 참고. 본 라운드 패치 14개 + 점검 결과 정리.

## 5. 베이스라인 영향

- pytest unit 전체 (`tests/unit/`, --ignore=fg33): 2747 passed, 13 skipped (Codex 3차 보강 후).
- security 250 passed, 1 skipped.

## 6. Phase 4 R5 contract drift 패턴 재사용 확인

`tests/integration/test_mcp_rest_drift.py` 가 보여준 "어떤 endpoint 가 의도된
권한 게이트를 우회하는가" 검증 패턴을 본 라운드에서는 **3축 결합** 으로 적용한다
(`FG6-4_Admin엔드포인트_점검표.md` §1 참고):

- (a) **helper 단위 분기** — `ensure_actor_can_access_org` 의 4 분기 모두 (8건).
- (b) **대표 route-level** — 4 endpoint group 각각 cross-org reject 1건 + SUPER_ADMIN
  cross-org audit emit 1건 (5건). agent / scope-profile / user-org-role / organization
  모듈을 모두 커버.
- (c) **코드 점검표** — 14 endpoint 각각의 guard 호출 wired 여부 review.

14 endpoint × 4 시나리오 전수 route-level 회귀는 본 라운드 범위 밖 (별 라운드).

## 7. 결론

R-O4 충족. 14 endpoint 격리 + SUPER_ADMIN 횡단 추적 (audit emit).
**FG 6-4 1차 종결**.
