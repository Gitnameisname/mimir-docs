# FG 6-1 검수보고서 — Rate Limit 일괄 적용

**작성일**: 2026-05-18
**Phase**: S3 / Phase 6 / FG 6-1
**Handoff Level**: extended

---

## 1. 검수 범위

- `backend/app/api/v1/annotations.py` — `@limiter.limit` 데코레이터 7개 엔드포인트 적용.
- `backend/app/api/v1/contributors.py` — 1 엔드포인트.
- `backend/app/api/v1/notifications.py` — 3 엔드포인트.
- 회귀: `tests/unit/test_rate_limit_fg61.py` (6건 — Codex P2-2 시정).

## 2. R-O1 (Rate Limit 단일) 점검

| 항목 | 결과 |
|---|---|
| 모든 핸들러가 `app.api.rate_limit.limiter` 의 동일 dependency 사용 | ✅ |
| 라우터 별도 `slowapi`/`Semaphore` 구현 | ✅ 없음 |
| limit 값이 plan §2 표와 일치 (30/60/120 per min) | ✅ |

## 3. 회귀 6건 결과 (Codex 2차 P2-2 시정)

```
$ .venv/bin/python -m pytest tests/unit/test_rate_limit_fg61.py
6 passed
```

| ID | 시나리오 | 결과 |
|---|---|---|
| L1 | annotations 7 핸들러가 wrapped (단일 테스트가 7 핸들러 일괄 검증) | ✅ |
| L2 | contributors 핸들러 wrapped | ✅ |
| L3 | notifications 3 핸들러 wrapped | ✅ |
| L4 | `_ANNOTATION_WRITE_LIMIT == "30/minute"` + `_ANNOTATION_READ_LIMIT == "60/minute"` | ✅ |
| L5 | `_NOTIFICATIONS_POLL_LIMIT == "120/minute"` | ✅ |
| L6 | `_CONTRIBUTORS_LIMIT == "60/minute"` | ✅ |

11 endpoint 의 wrapped 여부는 L1~L3 3개 테스트 안에서 일괄 단언된다 (한 테스트가
여러 endpoint 를 동시에 검증) — 테스트 함수 개수와 endpoint 개수의 1:1 매핑이
아닌 점이 Codex P2-2 의 카운트 차이 원인.

## 4. 베이스라인 영향

- pytest unit 전체 회귀 (`tests/unit/`, `--ignore=test_annotations_service_fg33.py`):
  **2739 passed, 13 skipped** — FG 6-1 인입으로 인한 추가 실패 0.
- `test_annotations_service_fg33.py` 의 사전 실패 4건 은 Phase 5 FG 5-5 의
  mention regex 변경 시점에 발생한 stale assertion (본 라운드 외 — Phase 5 종결
  메모리 참고).

## 5. 미완료 / 이월

- per-user (vs per-IP) keying — 본 라운드 보류. Phase 7 (Valkey) 와 함께 검토.
  Phase 계획서 §1.1 의 "per-user / per-IP rate-limit dependency 통일" 표현은
  현재 slowapi `_get_client_ip` 기반 (per-IP) 으로 통일되었음을 의미함 — Codex
  2차 §5 추가 관찰 반영 (per-user 는 별 라운드).
- cluster-wide 동기화 — Phase 7.

## 6. 결론

R-O1 충족. 11 endpoint 모두 `@limiter.limit` 적용. 회귀 녹색. **FG 6-1 1차 종결**.
