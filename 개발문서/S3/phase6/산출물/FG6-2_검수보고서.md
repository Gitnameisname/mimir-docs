# FG 6-2 검수보고서 — Retention 정책 + cron

**작성일**: 2026-05-18
**Phase**: S3 / Phase 6 / FG 6-2
**Handoff Level**: extended

---

## 1. 검수 범위

- `backend/app/db/migrations/versions/20260518_1200_s3_p6_retention_archive.py` — alembic.
- `backend/app/db/connection.py` — init_db bootstrap (dev/test).
- `backend/app/services/retention_job.py` — archive-first 트랜잭션.
- `backend/app/scheduler.py` — retention thread 통합.
- 회귀: `tests/unit/test_retention_job_fg62.py` (13건 — Codex 2차 P1-1 +3건, 3차 §4 audit verify +1건).

## 2. R-O2 (archive-first) 점검 — Codex 2차 P1-1 시정 후

| 항목 | 결과 |
|---|---|
| INSERT INTO archive → DELETE FROM source 단일 트랜잭션 | ✅ `WITH inserted AS (...) DELETE FROM ... WHERE id IN (deletable)` |
| 직접 DELETE 없음 (archive 안 거치는 row 없음) | ✅ `deletable = inserted ∪ already_archived` UNION |
| dry-run 모드에서 `commit()` 호출 0 | ✅ R1 회귀 |
| 답글 cascade — root + **임의 depth descendants** 함께 archive | ✅ `WITH RECURSIVE descendants AS (...)` (P1-1 시정) |
| archive 무결성 verify — DELETE 한 id 가 archive 에 존재하지 않으면 rollback | ✅ verify SELECT + RuntimeError + rollback (P1-1 + 3차 §4 audit 대칭화) |

## 3. 환경변수 검증

| 변수 | 기본 | override 테스트 |
|---|---|---|
| `RETENTION_DOCUMENT_VIEWED_DAYS` | 7 | ✅ R3 |
| `RETENTION_RESOLVED_ANNOTATION_DAYS` | 90 | (R9 default 확인) |
| `RETENTION_BATCH_LIMIT` | 500 | (값 직접 주입 회귀) |
| `RETENTION_DRY_RUN` | unset | ✅ R5 |
| `RETENTION_CRON_HOUR` | 2 | ✅ R7 / R8 |
| `RETENTION_BATCH_ENABLED` | true | (scheduler start_scheduler 분기 검증) |

## 4. 회귀 13건 결과 (Codex 3차 §4 audit verify 보강 후)

```
$ .venv/bin/python -m pytest tests/unit/test_retention_job_fg62.py
13 passed
```

| ID | 시나리오 | 결과 |
|---|---|---|
| R1 | dry-run no-op | ✅ |
| R2 | actual run commit ×2 (audit + annotations, 양쪽 verify 통과) | ✅ |
| R3 | **annotation archive-first 위반 시 rollback + error** (2차 P1-1) | ✅ |
| R4 | **annotation SQL 이 `WITH RECURSIVE descendants` 사용** (2차 P1-1) | ✅ |
| R5 | **audit SQL 이 `deletable = inserted UNION already_archived`** (2차 P1-1) | ✅ |
| R6 | **audit archive-first 위반 시 rollback + error** (3차 §4 audit verify 대칭화) | ✅ |
| R7 | env override | ✅ |
| R8 | env invalid fallback | ✅ |
| R9 | RETENTION_DRY_RUN truthy parse | ✅ |
| R10 | retention cron default `0 2 * * *` | ✅ |
| R11 | RETENTION_CRON_HOUR=5 → `0 5 * * *` | ✅ |
| R12 | RETENTION_CRON_HOUR=99 fallback | ✅ |
| R13 | 기본 일수 (7/90) constants | ✅ |

## 5. 마이그레이션 안전성

- `CREATE TABLE IF NOT EXISTS` — idempotent.
- 인덱스도 IF NOT EXISTS — 반복 실행 안전.
- downgrade 는 DROP TABLE IF EXISTS — 데이터 소실 가능. **운영 P1 승인 필수**.

## 6. 베이스라인 영향

- pytest unit 전체 (`tests/unit/`, --ignore=fg33): 2747 passed, 13 skipped (Codex 3차 보강 후).
- 신규 의존 0 (apscheduler 미사용 — stdlib threading 유지).

## 7. 운영 권고

1. 첫 배포 후 1~2일 `RETENTION_DRY_RUN=1` 로 운영 → log 의 candidate 카운트 검토.
2. 운영자 승인 후 dry-run 해제.
3. archive 자체의 sub-retention 은 별 라운드.

## 8. 결론

R-O2 충족. dry-run 가용 + audit 호환. **FG 6-2 1차 종결**.
