# FG 6-4 보안취약점검사보고서 — Admin API organization_id 격리

**작성일**: 2026-05-18
**Phase**: S3 / Phase 6 / FG 6-4

---

## 1. 검사 범위

OWASP A01:2021 (Broken Access Control) / API1:2023 (Broken Object Level
Authorization) — admin 권한 경계 결함 전수 점검.

## 2. 점검 결과

| # | 영역 | 위험 | 점검 | 상태 |
|---|---|---|---|---|
| S1 | ORG_ADMIN 이 다른 조직 scope_profile 생성/수정/삭제 | data tampering | `ensure_actor_can_access_org` | 차단 |
| S2 | ORG_ADMIN 이 다른 조직 agent 생성/수정/삭제 | data tampering + agent 권한 탈취 | 같음 | 차단 |
| S3 | ORG_ADMIN 이 다른 조직 user 에 role 부여/회수 | privilege escalation | 같음 (`assign_user_org_role`) | 차단 |
| S4 | ORG_ADMIN 이 다른 조직 organization 자체 수정/삭제 | tenant data loss | 같음 | 차단 |
| S5 | SUPER_ADMIN 횡단 시 audit 누락 → 탐지 불가 | observability | `admin.cross_org_access` emit | 차단 |
| S6 | list 응답에 다른 조직 자원이 섞임 | 정보 유출 | non-SUPER_ADMIN auto-scope | 차단 |
| S7 | target_org_id None 우회 (body 누락) | bypass | fail-closed (`raise ApiPermissionDeniedError`) | 차단 |
| S8 | 미인증 actor 가 admin write 호출 | 인증 우회 | `authorization_service.authorize` + 가드 이중 | 차단 |

## 3. 잔존 위험

| # | 위험 | 권고 |
|---|---|---|
| R1 | 다른 admin API (admin_alerts, admin_extraction 등) 의 org 경계 — 본 라운드 14 endpoint 외는 미점검 | 별 라운드 (검토 대상 목록 `FG6-4_Admin엔드포인트_점검표.md` §3) |
| R2 | 다중 ORG_ADMIN org 사용자의 list 응답 — 첫 org 으로 강제 | 별 ADR (다중 org admin UX) |
| R3 | SUPER_ADMIN 의 cross_org 빈도 알람 — 본 라운드는 emit 만, 알람 별 라운드 | Phase 7 (Valkey + observability) |

## 4. 회귀

- `tests/unit/test_admin_org_guard_fg64.py` 8건 ✅
- security 250 passed ✅
- pytest unit 베이스라인 영향 없음.

## 5. 결론

OWASP A01 / API1 보호 layer 추가. 14 endpoint 격리 + audit 추적.
**Critical/High 0, Medium 0, Low 3 (R1/R2/R3)**.
**FG 6-4 보안 1차 종결**.
