# FG 6-2 보안취약점검사보고서 — Retention 정책

**작성일**: 2026-05-18
**Phase**: S3 / Phase 6 / FG 6-2

---

## 1. 검사 범위

OWASP A02:2021 (Cryptographic Failures — 본 사례에선 PII 잔존 측면) /
A04:2021 (Insecure Design — 데이터 폐기 정책).

## 2. 점검 결과

| # | 영역 | 위험 | 점검 | 상태 |
|---|---|---|---|---|
| S1 | audit_events.document.viewed 무기한 보존 | PII 잔존 / GDPR 28조 위반 가능 | 7일 archive | 차단 |
| S2 | resolved annotation 무기한 보존 | 같은 위험 + 사용자 멘션 흔적 | 90일 archive | 차단 |
| S3 | retention 가 의도보다 많은 row 삭제 (off-by-one) | 정상 데이터 손실 | dry-run 모드 + candidate 카운트 | 차단 |
| S4 | DELETE 만 실행되고 archive 누락 | 데이터 영구 손실 | 단일 트랜잭션 (`WITH archived AS ... DELETE`) | 차단 |
| S5 | scheduler 다중 워커 race | duplicated archive INSERT | `ON CONFLICT (id) DO NOTHING` | 차단 |
| S6 | archive 테이블 권한 — 일반 사용자 접근 | PII 노출 | API 노출 0. 운영자 SQL 만 | 차단 |
| S7 | 환경변수 mis-set (예: `RETENTION_DOCUMENT_VIEWED_DAYS=0`) | 즉시 모든 데이터 archive | int 파싱 + fallback. 값이 0 이면 즉시 archive 되는 동작은 명시 — 운영자 책임 | 권고 |

## 3. 잔존 위험 (운영 권고)

| # | 위험 | 권고 |
|---|---|---|
| R1 | archive 테이블이 빠르게 거대화 | archive 자체에 sub-retention (1년 cold storage / 2년 PURGE) — 별 라운드 |
| R2 | 운영자가 `RETENTION_DOCUMENT_VIEWED_DAYS=0` 으로 설정 시 첫 cron 에 모든 view audit archive | 환경변수 변경은 P1 — `AGENT_MODE.md` §5 추가 권장 |
| R3 | scheduler thread 죽으면 retention 정지. APScheduler 부재 환경 폐쇄망 호환 유지 | 별 헬스 메트릭 — Phase 7 와 함께 검토 |

## 4. 회귀

- `tests/unit/test_retention_job_fg62.py` 9건 ✅
- pytest unit 베이스라인 영향 없음.

## 5. 결론

OWASP A02 / A04 보호 layer 추가. PII 잔존 차단 + 데이터 손실 방어 (archive-first).
**Critical/High 0, Medium 0, Low 1 (R2 — 환경변수 변경 절차)**.
**FG 6-2 보안 1차 종결**.
