# FG 3-2 종결 보고서

**FG**: S3 Phase 3 FG 3-2 — 열람자 노출 제어
**완료일**: 2026-04-27
**상태**: 종결 (final review 대기)

---

## 1. 작업 개요

- `scope_profiles.settings_json` JSONB 컬럼 신설 + `expose_viewers` 첫 키 (default false)
- `should_expose_viewers` fail-closed 정책 게이트
- Contributors API (FG 3-1) 와 결합 — 사용자 의사 AND 정책 둘 다 true 일 때만 viewers 노출
- Admin PATCH `/admin/scope-profiles/{id}` 에 settings 처리 + audit emit + 캐시 invalidate
- viewer 노출 우회 경로 10 곳 전수 점검 (우회 0)

## 2. 산출물 체크리스트

| 항목 | 상태 | 위치 |
|-----|-----|-----|
| settings_json 마이그레이션 | ✅ | `db/connection.py` `_SCOPE_PROFILES_SETTINGS_JSON_MIGRATION_DDL` |
| ScopeProfileSettings dataclass | ✅ | `models/scope_profile.py` |
| Repository round-trip | ✅ | `repositories/scope_profile_repository.py` |
| 정책 게이트 + 캐시 | ✅ | `services/scope_profile_policy.py` |
| Contributors API 결합 | ✅ | `services/contributors_service.py` |
| Admin PATCH + audit + invalidate | ✅ | `api/v1/scope_profiles.py` |
| viewer 노출 경로 점검표 | ✅ | `산출물/FG3-2_viewer노출경로_점검표.md` |
| Backend 단위 테스트 | ✅ | 33 신규 (settings 19 + policy 14) |
| Frontend type / API 갱신 | ✅ | `types/s2admin.ts`, `lib/api/s2admin.ts` |
| Admin UI 토글 | ✅ | `features/admin/scope-profiles/AdminScopeProfilesPage.tsx` |
| Frontend 단위 테스트 | ✅ | 7 신규 |
| UI 검수 로그 | ✅ | `산출물/FG3-2_UI리뷰로그.md` |
| 검수 보고서 | ✅ | `산출물/FG3-2_검수보고서.md` |
| 보안 취약점 검사 | ✅ | `산출물/FG3-2_보안취약점검사보고서.md` |

## 3. 게이트 결과 (작업지시서 §6)

| 기준 | 결과 |
|-----|-----|
| `scope_profiles.settings_json` 마이그레이션 + `{}` 검증 | ✅ DEFAULT '{}'::jsonb |
| `expose_viewers` default false (모든 기존 profile) | ✅ 3 단계 방어 (DDL DEFAULT + dataclass default + fail-closed _from_raw) |
| Contributors API 결합 회귀 | ✅ 4 신규 unit (정책/사용자 AND, gate 호출, both-true) |
| viewer 노출 경로 점검표 우회 0 | ✅ 10 경로 전수 점검 |
| 관리자 UI 토글 + audit emit | ✅ |
| pytest 신규 ≥ 15 | ✅ 33 신설 |
| node:test 신규 ≥ 6 | ✅ 7 신설 |
| 보안 보고서 + 기본값 off 검증 | ✅ §2.5 마이그레이션 안전성 + §5 회귀 |

## 4. 회귀

- pytest: 신규 33 추가, 기존 회귀 0 (FG 3-1 유지)
- node:test: 533 → 540 (+7, 회귀 0)
- tsc: 0 touched 오류

## 5. 잔존 / 후속

| # | 항목 | 라운드 |
|---|-----|-----|
| 1 | admin 다른 조직 profile 수정 가능 (T5) | 별 라운드 — 전체 admin API 일괄 |
| 2 | 다중 워커 cluster-wide cache invalidation | Phase 4 (Valkey pub/sub) |
| 3 | document.viewed retention 정책 | 운영 도입 시 |
| 4 | settings 의 다음 키 (allow_agent_actions 등) | 향후 FG |

## 6. 다음 task

- **task3-3 — 인라인 주석 + 멘션 + MCP** (Phase 3 의 마지막 FG, S3 1라운드 종결 회고 포함)

---

*작성: 2026-04-27 | task3-2 종결*
