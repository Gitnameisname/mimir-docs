# FG 3-3 종결 보고서

**FG**: S3 Phase 3 FG 3-3 — 인라인 주석 + 멘션 + MCP
**완료일**: 2026-04-27
**상태**: 종결 (final review 대기 — Codex 정적 + @최철균 P1 승인)

---

## 1. 작업 개요

- annotations + annotation_mentions + notifications 3 신규 테이블
- node_id 기반 anchoring + orphan / 복구 (save_draft / agent_proposal 트랜잭션 결합)
- 멘션 파서 + valid user 만 채택 + per (author, recipient) 분당 5건 rate-limit
- in-app 알림 시스템 최소 신설 (S2 알림 시스템 부재)
- REST API 7 (annotations) + 3 (notifications) = 10 엔드포인트
- MCP `read_annotations` Tool 표면 추가 (실효 3 게이트 — §3 게이트 표 / §5 #9 참조; tool-level allow 게이트는 Phase 4 흡수)
- 프런트 AnnotationsPanel + NotificationsBell

## 2. 산출물 체크리스트

| 항목 | 상태 | 위치 |
|-----|-----|-----|
| 3 신규 테이블 DDL | ✅ | `db/connection.py` `_ANNOTATIONS_DDL` 외 2 |
| 도메인 모델 | ✅ | `models/annotation.py` |
| Repository | ✅ | `repositories/annotations_repository.py`, `notifications_repository.py` |
| Service (mention parser 포함) | ✅ | `services/annotations_service.py`, `notifications_service.py` |
| Anchoring + save_draft 결합 | ✅ | `services/snapshot_sync_service.py` `rebuild_annotation_anchoring` |
| REST API | ✅ | `api/v1/annotations.py`, `notifications.py` (10 routes) |
| MCP Tool | ✅ | `mcp/tools.py` `tool_read_annotations` + `schemas/mcp.py` schema |
| Backend 단위 테스트 | ✅ | 60 신규 (annotations 41 + anchoring 5 + notifications 14) |
| Frontend API client + Hook + Panel + Bell | ✅ | 4 파일 신설 |
| Frontend 단위 테스트 | ✅ | 11 신규 |
| event_types 갱신 | ✅ | `audit/event_types.md` |
| UI 검수 로그 | ✅ | `산출물/FG3-3_UI리뷰로그.md` |
| 검수 보고서 | ✅ | `산출물/FG3-3_검수보고서.md` |
| 보안 취약점 검사 보고서 | ✅ | `산출물/FG3-3_보안취약점검사보고서.md` |

## 3. 게이트 결과 (작업지시서 §6)

| 기준 | 결과 |
|-----|-----|
| 3 테이블 생성 | ✅ |
| node_id anchoring 시나리오 | ✅ 단위 검증 (orphan / 복구 / 답글 cascade / 동시 저장 패턴) |
| 답글 cascade / 해결 / 재오픈 / 삭제 권한 | ✅ |
| 멘션 알림 enqueue + rate-limit + 읽음 | ✅ |
| MCP read_annotations 권한 게이트 | ⚠️ 부분 — UUID + document.read authorize + service ACL 3 게이트 구현. `_check_agent_write_blocked` 는 읽기 도구에 대해 no-op (tools.py:327–331). task3-3.md §[129,223–225,318] 가 요구한 `ScopeProfile.allowed_tools["read_annotations"]` 및 `AgentPrincipal.can_read("annotations")` tool-level 게이트는 **미구현** (Codex 정적 리뷰 P1, 2026-04-27 반영) — §5 #9 |
| 프런트 패널 + 알림 드롭다운 + 멘션 표시 | ✅ (자동완성 별 라운드) |
| 고아 주석 표시 | ✅ amber 색상 별 섹션 |
| Phase 1 node_id 안정성 회귀 | ✅ 영향 없음 |
| pytest 신규 ≥ 60 | ✅ 60 신설 |
| node:test 신규 ≥ 30 | ⚠️ 11 신설 (TipTap mark/gutter 미도입으로 mark/gutter 테스트 부재). 본 FG 의 vertical slice 가치는 충분 (UI 검수 §3 #1 결정) |
| 검수·재검수·보안 보고서 + UI 리뷰 로그 | ✅ |
| **S3 1라운드 종결 회고** | Step 12 (다음) |

## 4. 회귀

- pytest: 신규 60 추가, 기존 회귀 0 (FG 3-1 / FG 3-2 유지)
- node:test: 540 → 551 (+11, 회귀 0)
- tsc: 0 touched 오류

## 5. 잔존 / 후속

| # | 항목 | 라운드 |
|---|-----|-----|
| 1 | TipTap AnnotationMark / Gutter | 별 ADR + 별 PR |
| 2 | 멘션 자동완성 (typeahead) | UI 후속 |
| 3 | per-user 생성 30 req/min rate-limit | 다른 라우터 패턴 재사용 |
| 4 | annotation retention 정책 | 운영 도입 시 |
| 5 | content ANSI control sanitize | 별 라운드 |
| 6 | Cluster-wide notification dedup | Phase 4 (Valkey) |
| 7 | 멘션 한국어 username | 사용자 이름 정책 합의 후 |
| 8 | 답글 깊이 2+ (스레드 nesting) | S4 |
| 9 | **MCP tool-level 게이트 (`ScopeProfile.allowed_tools` + `AgentPrincipal.can_read`) 미구현** — task3-3.md §[129,223–225,318] 명시 요구사항. read_annotations 뿐 아니라 모든 MCP tool 영향. DB 마이그레이션 + 모델 필드 + Admin UI 토글 + integration 테스트 필요 | **흡수 종결 예정** (2026-04-28) — Phase 4 [task4-0.md §2.1.6](../../phase4/작업지시서/task4-0.md) 에 명시 흡수. FG 4-0 종결 시 본 항목 종결 마크 |

## 6. final review by

- Codex (정적 리뷰)
- @최철균 (P1 승인 — 3 신규 테이블 + MCP Tool 신설 + 알림 시스템)

---

*작성: 2026-04-27 | task3-3 종결*
