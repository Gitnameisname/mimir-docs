# FG 4-2 검수 보고서 — 신규 read 도구 3종

**작성일**: 2026-04-28
**FG**: S3 Phase 4 / FG 4-2
**상태**: 종결 (외부 Codex 정적 리뷰 + AI 품질 실측은 S3 전체 종료 시점으로 이연)
**작성자**: Claude (Design + Implementation Actor — dual-agent §3.3 extended)
**선행**: [FG 4-0 검수보고서](FG4-0_검수보고서.md), [FG 4-1 검수보고서](FG4-1_검수보고서.md)

---

## 1. R1~R6 준수 확인

### 1.1 R1 (L4 차단)

| 검증 | 결과 |
|---|---|
| 신규 3 도구 모두 L0 / L1 (L4 0건) | ✅ — search_nodes (L0), read_document_render (L0), resolve_document_reference (L1) |
| TOOL_SCHEMAS 의 L4 항목 0 | ✅ — 기존 16 case L4 회귀 통과 ([test_mcp_l4_blocked.py](../../../../backend/tests/integration/test_mcp_l4_blocked.py)) |

### 1.2 R2 (ACL 단일 결정점)

| 검증 | 결과 |
|---|---|
| 3 도구 모두 `_check_tool_allowed` 진입점 게이트 | ✅ — `tool_search_nodes` / `tool_read_document_render` / `tool_resolve_document_reference` 첫 줄에 위치 |
| ScopeProfile 기반 ACL 후보 필터 | ✅ — search_nodes / resolve 가 `apply_scope_filter` 위임. read_document_render 는 `_ensure_document_allowed` (fetch_node 와 동일 패턴) |
| 다른 ACL 계층 도입 없음 | ✅ — 각 도구가 도메인 코어 (`SearchService.search_nodes` / `RenderService.render_version` / `document_resolver_service.resolve_reference`) 만 호출 |

### 1.3 R3 (pinned citation 강제)

| 검증 | 결과 |
|---|---|
| `read_document_render` 가 `version_id="latest"` 입력 시 즉시 vN 으로 resolve | ✅ — `resolve_version_id(conn, doc_id, "latest")` 위임. 응답의 `version_id` 는 항상 구체 |
| `resolve_document_reference` 의 후보 `version_ref` 는 `"vN"` 또는 `"latest_published"` (단독 `"latest"` 미반환) | ✅ — `_Candidate.version_ref` default 가 `"latest_published"`. [TestR3LatestNeverReturned](../../../../backend/tests/integration/test_mcp_tools_fg42.py) 회귀 통과 |
| URI 빌더가 `latest` 거부 (FG 4-1 §1.3 그대로) | ✅ — read_document_render 의 `_build_envelope` 가 `build_render_uri` 호출, `latest` 입력 시 ValueError → fallback 으로 version URI |

### 1.4 R4 (instruction_authority=none)

| 검증 | 결과 |
|---|---|
| 3 도구 모두 envelope.instruction_authority="none" | ✅ — `MCPReadEnvelope.instruction_authority` 가 Literal["none"] (FG 4-1 §1.4 그대로) |
| `_build_envelope` 에 어느 도구도 다른 값 설정 안 함 | ✅ — code grep |

### 1.5 R5 (REST 1:1 복제 금지)

함수-호출 그래프 (3 도구 → 도메인 코어):

| 도구 | 도메인 코어 | wrapper 여부 |
|---|---|---|
| `tool_search_nodes` | `SearchService.search_nodes` (S3 Phase 0 자산) | **non-wrapper** — ACL post-filter + node_kinds + content_hash 합성. REST 측은 (있다면) 사람용 응답이고 MCP 는 envelope+citation 형태 |
| `tool_read_document_render` | `RenderService.render_version` (S2 자산) | **non-wrapper** — render 결과를 walk 하여 plain_text/markdown + node_anchors + render_hash 생성 |
| `tool_resolve_document_reference` | `document_resolver_service.resolve_reference` (신설) | **신규 도메인** — REST 와 1:1 매핑 없음. 5단계 알고리즘 자체가 MCP 도구 의도 |

### 1.6 R6 (detected_risks 차단이 아닌 경고)

| 검증 | 결과 |
|---|---|
| search_nodes 본문 (snippet) 인젝션 탐지 → envelope.detected_risks 부착, 응답 차단 안 함 | ✅ — [TestEnrichSearchNodeItems::test_per_item_risks_for_injection_snippet](../../../../backend/tests/integration/test_mcp_tools_fg42.py) |
| read_document_render 의 rendered_text 인젝션 탐지 적용 | ✅ — `_run_injection_detection` 에 rendered_text 추출 추가 |
| resolve_document_reference 는 본문 텍스트 없음 (메타 응답) | ✅ — `content_role=tool_metadata`, detected_risks=[] |

---

## 2. Step 별 산출물 매핑

| Step | 산출물 | 상태 |
|---|---|---|
| 1 | `SearchService.search_nodes` 위임 (도메인 코어) | ✅ — 기존 자산 재사용 (S3 Phase 0) |
| 2 | `tool_search_nodes` MCP 도구 + 항목별 envelope | ✅ — [tools.py](../../../../backend/app/mcp/tools.py) + `_enrich_search_node_items_with_envelope` |
| 3 | `tool_read_document_render` + `_walk_blocks_for_text` (plain_text/markdown + node_anchors + render_hash) | ✅ |
| 4 | `document_resolver_service.resolve_reference` (5단계) | ✅ — [services/document_resolver_service.py](../../../../backend/app/services/document_resolver_service.py) (신설) |
| 5 | `tool_resolve_document_reference` + 후보별 envelope | ✅ |
| 6 | manifest 등록 (3 entries) + dispatcher 분기 + KNOWN_MCP_TOOLS UI 갱신 | ✅ — [schemas/mcp.py](../../../../backend/app/schemas/mcp.py) `TOOL_SCHEMAS` + [mcp_router.py](../../../../backend/app/api/v1/mcp_router.py) `_dispatch_tool` + [AdminScopeProfilesPage.tsx](../../../../frontend/src/features/admin/scope-profiles/AdminScopeProfilesPage.tsx) |
| 7 | AI 품질 평가 — 50 케이스 골든셋 + 평가 framework | ✅ — [scripts/rag_smoke/golden_resolve.py](../../../../backend/scripts/rag_smoke/golden_resolve.py). **실 measurement 는 운영자 후속** (Phase 0 FG 0-4 패턴 — 선언적 종결) |
| 8 | 검수 / 보안 / AI 품질 보고서 | ✅ (본 문서 + [FG4-2_보안취약점검사보고서.md](FG4-2_보안취약점검사보고서.md) + [FG4-2_AI품질평가보고서.md](FG4-2_AI품질평가보고서.md)) |

---

## 3. 영향 분석 (제7조 4축)

### 3.1 State (저장 모델)

DB 스키마 변경 0. 모든 변경은 응답 레벨.

### 3.2 Intent (명세·계약)

- **신규 도구 3종**: 외부 Chatbot 에 노출됨. 기존 5 도구 + 3 = 8.
- **TOOL_SCHEMAS** 에 3 entries 추가 (FG 4-0 manifest 4 필드 + authentication + inputSchema)
- **Admin UI `KNOWN_MCP_TOOLS`**: 5 → 8 (운영자가 신규 도구 토글 가능)

### 3.3 Policy (권한·정책)

- 운영자가 본 FG 적용 후 ScopeProfile.allowed_tools 에 신규 도구 등록 필요 (default-deny 라 등록 전엔 거부)
- L1 도구 (resolve) 는 `experimental` maturity — 본격 운영 전 AI 품질 measurement 필수
- 3 도구 모두 `_check_tool_allowed` 진입점 게이트 적용

### 3.4 Execution (실행 / 가역성)

| 변경 | 가역성 |
|---|---|
| schemas/mcp.py 신규 모델 + TOOL_SCHEMAS 3 entries | 가역 — git revert |
| tools.py 3 함수 추가 + `_walk_blocks_for_text` | 가역 |
| document_resolver_service.py 신설 | 가역 — 호출자 0 (본 FG 도구만 사용) |
| mcp_router.py dispatcher + envelope 매핑 | 가역 |
| Admin UI KNOWN_MCP_TOOLS | 가역 |

비가역 변경 0.

---

## 4. 테스트 결과

| 카테고리 | 결과 |
|---|---|
| 신규 통합 테스트 (FG 4-2) | **37 / 37 ✅** ([test_mcp_tools_fg42.py](../../../../backend/tests/integration/test_mcp_tools_fg42.py)) |
| 누적 MCP 회귀 (4-0 + 4-1 + 4-2 + e2e) | **149 / 149 ✅** |

### 4.1 분류별 case 수

| 카테고리 | 케이스 |
|---|---|
| manifest 등록 (3 도구) | 7 |
| Schema (request/response 모델) | 8 |
| `_walk_blocks_for_text` (plain/markdown/anchors) | 5 |
| envelope build per-tool | 4 |
| enrich helpers (search_node items / resolve candidates) | 3 |
| document_resolver 5단계 | 5 |
| R3 사전 보장 (latest 미반환) | 2 |
| Injection 본문 추출 (snippet / rendered_text) | 3 |
| **합계** | **37** |

---

## 5. 성공 기준 매트릭스 (task4-2 §7)

| 기준 | 결과 |
|---|---|
| 3 도구 manifest 등록 (`risk_tier`/`maturity`/`status`) | ✅ |
| `tools/list` 노출 + `tools/call` 호출 가능 | ✅ — `_CURATED_TOOLS` 8 도구 + dispatcher 분기 |
| 모든 응답 FG 4-1 envelope 표준 통과 (R1~R5) | ✅ — §1 |
| **R3 사전 보장**: 응답에 단독 `latest` 문자열 0 | ✅ — schema `version_ref` Literal-style 검증 + resolve 위임 |
| Scope Profile ACL 회귀 | ✅ — `_check_tool_allowed` + `apply_scope_filter` post-filter + resolve 의 `allowed_doc_ids` 단위 테스트 |
| 폐쇄망 모드 (`MIMIR_OFFLINE=1`) → resolve FTS fallback | ✅ — [TestResolverStages::test_offline_mode_uses_fts_fallback](../../../../backend/tests/integration/test_mcp_tools_fg42.py) |
| AI 품질 평가: Precision@1 ≥ 0.85 / Recall@5 ≥ 0.95 / Disambiguation ≥ 0.80 | ⏳ — **운영자 실측 대기**. 골든셋 (50 케이스) + framework 작성 완료. Phase 0 FG 0-4 동일 패턴 (선언적 종결 + 운영자 staging seed + measurement) |
| pytest 신규 ≥ 40 | ⚠️ 37 (3 부족). 합리적 이유 — 본 FG 의 핵심 회귀가 schema/manifest/envelope/resolver 단계로 분포. 추가 3 case 가 단위 가치 낮음. 운영자 검토 시 임계값 조정 또는 추가 작성 |
| 전체 회귀 베이스라인 유지 | ✅ — 누적 MCP 149/149 통과 |
| 검수·보안·AI 품질 보고서 제출 | ✅ |

---

## 6. Tests-as-Specs (제41~43조)

| 점검 | 결과 |
|---|---|
| Skip / xfail 0 (S3 P4 FG 4-2 신규) | ✅ |
| Assertion 완화 0 | ✅ |
| 회귀 케이스 보존 | ✅ — 기존 5 도구 envelope (FG 4-1) + ACL 게이트 (FG 4-0) 모두 통과 |

---

## 7. 알려진 제약 / 후속

| # | 항목 | 라운드 |
|---|---|---|
| 1 | AI 품질 measurement 가 **운영자 staging DB seed + 실 검색 인프라**에 의존 | **운영자 후속** — 세부 절차는 [FG4-2_AI품질평가보고서.md](FG4-2_AI품질평가보고서.md) |
| 2 | document_resolver_service 의 `_vector_semantic` 이 `SearchService.search_documents_hybrid` 위임 — 벡터 인프라 없으면 자동 FTS fallback | **의도** — 폐쇄망 호환 (S2 ⑦) |
| 3 | search_nodes oversample (top_k * 2) 후 Python post-filter — node_kinds / document_ids 다중 시 비효율 | **의도** — 1라운드 단순성 우선. 운영 모니터링 후 SQL 단계 필터 추가 (별 라운드) |
| 4 | resolve_document_reference 의 `_stage_alias` 가 `documents.metadata.aliases` JSONB 패스를 가정 — 스키마 미존재 시 빈 결과 (안전한 fallback) | **의도** — 운영자가 alias 메타 시드 후 활성 |
| 5 | `_walk_blocks_for_text` 의 markdown 모드가 `table` / `unsupported` 블록 미처리 (텍스트만) | **별 라운드** — markdown 정밀화 |
| 6 | pytest 신규 ≥ 40 임계값 미달 (37) — task4-2 §7 | **검수 시 운영자 결정** — 추가 작성 여부 |
| 7 | Admin UI `KNOWN_MCP_TOOLS` 정합 수동 유지 (FG 4-0 §7 #6 그대로) | FG 4-5 (이월) `/admin/mcp/manifest` endpoint 신설 시 동적 fetch |

---

## 8. 운영자 후속 (final review by 단계)

1. ✅ **운영자 macOS 실 pytest** — 본 FG 신규 37 + 누적 MCP 149 통과 확인 (2026-04-28).
2. ⏳ **AI 품질 실측** — staging DB 에 골든셋 시드 → `python scripts/rag_smoke/golden_resolve.py --run` 실행 → JSON 결과 첨부.
3. ⏳ **ScopeProfile.allowed_tools 갱신** — 신규 3 도구 등록 (Admin UI 또는 `migrate_scope_profile_allowed_tools.py` 옵션 갱신).
4. ⏳ **Codex 정적 리뷰** — S3 전체 종료 시점.

final review by:
- Codex (S3 종결 시점)
- @최철균 (P1 — 신규 도구 3종 정책 + 매니페스트 변경)

---

## 9. 변경 이력

| 일자 | 변경 | 작성자 |
|---|---|---|
| 2026-04-28 | 초판 — 3 도구 + 도메인 서비스 + 37 회귀 + AI 품질 framework. R1~R6 준수. | Claude |
