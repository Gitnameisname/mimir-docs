# FG 4-5 검수 보고서 — Capability Manifest 확장

**작성일**: 2026-04-28
**FG**: S3 Phase 4 / FG 4-5 (이월 — 사용자 합의 후 진입, 2026-04-28 진입 결정)
**상태**: 종결 (Codex 정적 리뷰는 S3 종결 시점)
**작성자**: Claude (Design + Implementation Actor — dual-agent §3.3 extended)
**선행**: [FG 4-0 ~ 4-4 검수보고서](.) (Phase 4 1라운드 종결)

---

## 1. R1~R6 영향

본 FG 는 **메타데이터 표면화** — 신규 도구 / DB / 권한 추가 0. R1~R6 직접 영향 없음.

| R | 영향 | 보장 |
|---|---|---|
| R1 (L4 차단) | 0 | TOOL_SCHEMAS L4 0건 유지 — 기존 회귀 그대로 |
| R2 (ACL 단일) | 0 | ScopeProfile.allowed_tools 모델 변경 없음 |
| R3 (pinned) | 0 | verify_citation 동작 그대로 |
| R4 (instruction_authority=none) | 0 | envelope Literal 변경 없음 |
| R5 (REST 1:1 복제 금지) | 0 | 신규 도구 0 |
| R6 (detected_risks 경고) | 0 | risk_mapper 동작 그대로 |

---

## 2. Step 별 산출물

| Step | 산출물 | 상태 |
|---|---|---|
| 1 | schemas/mcp.py — 5 신규 필드 + Literal `PolicyProfile` + 8 도구 default 부착 | ✅ — [schemas/mcp.py](../../../../backend/app/schemas/mcp.py) |
| 2 | `mcp_exposed_public_view` 외부 노출 정책 (requires/preferred_use/streaming_supported 포함, default_enabled/policy_profile 비노출) | ✅ |
| 2 | `mcp_admin_full_view` 운영자 전용 view | ✅ |
| 2 | `default_enabled_tool_names` / `tools_by_policy_profile` 헬퍼 | ✅ |
| 3 | `GET /admin/mcp/manifest` endpoint (admin.read 게이트) | ✅ — [api/v1/admin.py](../../../../backend/app/api/v1/admin.py) |
| 4 | `ScopeProfile.create(use_defaults: bool = False)` 옵션 | ✅ — [scope_profile_repository.py](../../../../backend/app/repositories/scope_profile_repository.py) |
| 5 | `dump_mcp_manifest.py` — 5 신규 필드 + by_policy_profile 통계 | ✅ — [scripts/dump_mcp_manifest.py](../../../../backend/scripts/dump_mcp_manifest.py) (schema_version 1.0 → 1.1) |
| 5 | `MCP_도구_매니페스트.json` 재생성 + drift 게이트 통과 | ✅ |
| 6 | API client `mcpManifestApi` + types `AdminMcpToolEntry` | ✅ — [s2admin.ts](../../../../frontend/src/lib/api/s2admin.ts) |
| 6 | Admin UI 동적 fetch — `KNOWN_MCP_TOOLS` 정적 상수 제거 | ✅ — [AdminScopeProfilesPage.tsx](../../../../frontend/src/features/admin/scope-profiles/AdminScopeProfilesPage.tsx) |
| 6 | UI 표시: policy_profile 색상 + default 표시 + stream 표시 + requires 텍스트 + preferred_use 툴팁 | ✅ |
| 7 | 검수 / 보안 보고서 | ✅ (본 문서 + [FG4-5_보안취약점검사보고서.md](FG4-5_보안취약점검사보고서.md)) |

---

## 3. 8 도구 5 필드 적용 매트릭스

| 도구 | risk_tier | maturity | default_enabled | requires | policy_profile | streaming_supported |
|---|---|---|---|---|---|---|
| search_documents | L0 | stable | ✅ | [] | read_safe | ✅ |
| fetch_node | L0 | stable | ✅ | [] | read_safe | ❌ |
| verify_citation | L1 | stable | ✅ | [] | read_safe | ❌ |
| read_annotations | L0 | beta | ✅ | [] | read_safe | ❌ |
| search_nodes | L0 | beta | ✅ | [] | read_safe | ❌ |
| read_document_render | L0 | beta | ✅ | [] | read_safe | ❌ |
| mimir.vectorization.status | L0 | beta | ❌ | [] | read_safe | ❌ |
| resolve_document_reference | L1 | experimental | ❌ | [] | experimental | ❌ |

`default_enabled=True`: **6개** — `use_defaults=True` 신규 ScopeProfile 자동 등록 후보.
`default_enabled=False`: **2개** — 운영 진단용(vectorization.status) + experimental(resolve).

---

## 4. 외부 / 운영자 view 분리 정책

도구등급화_매핑.md §4 옵션 A 의 보수 정책 그대로:

| 필드 | 외부 (`tools/list`) | 운영자 (`/admin/mcp/manifest`) |
|---|---|---|
| name / description | ✅ | ✅ |
| maturity | ✅ | ✅ |
| **requires** (FG 4-5 신규) | ✅ | ✅ |
| **preferred_use** (FG 4-5 신규) | ✅ | ✅ |
| **streaming_supported** (FG 4-5 신규) | ✅ | ✅ |
| risk_tier | ❌ | ✅ |
| status | ❌ | ✅ |
| exposure_policy | ❌ | ✅ |
| **default_enabled** (FG 4-5 신규) | ❌ | ✅ |
| **policy_profile** (FG 4-5 신규) | ❌ | ✅ |
| authentication / inputSchema | ✅ | ✅ |

근거: `default_enabled` / `policy_profile` 은 운영 정찰 가능성 (외부 클라이언트가 어떤 도구가 default 인지 / 어떤 정책 그룹인지 추론) — 운영자 전용. `requires` 는 클라이언트 의존성 인지 필요, `preferred_use` 는 외부 안내 가치, `streaming_supported` 는 클라이언트 stream endpoint 분기 필요.

---

## 5. 영향 분석 (제7조 4축)

### 5.1 State

DB 스키마 변경 0 — manifest 는 코드 상수.

### 5.2 Intent (계약)

- `tools/list` 외부 응답에 `requires` / `preferred_use` / `streaming_supported` 추가 (Optional / 기본값으로 호환)
- `GET /admin/mcp/manifest` endpoint 신규 (admin.read 권한)
- `ScopeProfileCreate` 의 `use_defaults: bool = False` 추가 (Optional, 기본 false → 기존 동작 보존)

### 5.3 Policy

- `default_enabled` 가 ScopeProfile 자동 등록 정책 표면화 — 운영자가 명시 옵트인 (`use_defaults=True`) 후 적용
- `policy_profile` 그룹화는 메타만 — 런타임 정책 분기는 별 라운드

### 5.4 Execution (가역성)

| 변경 | 가역성 |
|---|---|
| 5 manifest 필드 + 헬퍼 | 가역 — git revert |
| `/admin/mcp/manifest` endpoint | 가역 — endpoint 제거 |
| `ScopeProfile.create(use_defaults)` | 가역 — 기존 호출자 영향 0 (default false) |
| Admin UI 동적 fetch | 가역 — KNOWN_MCP_TOOLS 정적 상수 복원 가능 |
| `dump_mcp_manifest` schema_version 1.0 → 1.1 | 가역 (CI drift 게이트가 자동 정합) |

비가역 변경 0.

---

## 6. 테스트 결과

| 카테고리 | 결과 |
|---|---|
| 신규 통합 테스트 (FG 4-5) | **27 / 27 ✅** ([test_mcp_manifest_extended_fg45.py](../../../../backend/tests/integration/test_mcp_manifest_extended_fg45.py)) |
| 누적 MCP 회귀 (4-0 ~ 4-5 + e2e) | **240 / 240 ✅** |
| Manifest drift 게이트 | ✅ — 5 신규 필드 정합 |
| 전체 회귀 | **3522 passed / 126 skipped / 0 failed** |

### 6.1 분류별 case 수

| 카테고리 | 케이스 |
|---|---|
| 5 신규 필드 정합 (8 도구) | 6 |
| policy_profile 분류 | 2 |
| default_enabled 정책 | 3 |
| streaming_supported | 2 |
| 헬퍼 (default_enabled_tool_names / tools_by_policy_profile) | 5 |
| public view 외부/운영자 분리 | 3 |
| ScopeProfile.create(use_defaults) | 3 |
| Admin endpoint 구조 | 2 |
| manifest drift FG 4-5 정합 | 1 |
| **합계** | **27** |

---

## 7. Tests-as-Specs (제41~43조)

| 점검 | 결과 |
|---|---|
| Skip / xfail 0 (FG 4-5 신규) | ✅ |
| Assertion 완화 0 | ✅ |
| 회귀 케이스 보존 | ✅ — 누적 MCP 213 → 240 (전체 보존) |

---

## 8. 헌법 / 원칙 정합성

| 조항 | 적용 |
|---|---|
| 제5조 Agent-Facing Contract | preferred_use / requires / streaming_supported 가 도구 계약 표면화 |
| 제15조 Bounded Change | 본 FG 가 메타데이터만 — DB / 권한 / 신규 도구 변경 0 |
| 제18조 Provenance | manifest 의 정책 표면 (policy_profile) 이 운영 의도 출처 명시 |
| 제29조 extended Handoff | design + implementation = Claude, review = Codex (S3 종결), human approval = @최철균 |

---

## 9. 알려진 제약 / 후속

| # | 항목 | 라운드 |
|---|---|---|
| 1 | `requires` 의 런타임 의존성 검사 (도구 호출 시점에 의존성 충족 검증) | 별 라운드 — 본 FG 는 메타만 |
| 2 | `policy_profile` 별 런타임 정책 분기 (예: write_audited 그룹 idempotency 강제) | FG 4-6 의 일부 + 일반화는 별 라운드 |
| 3 | Admin UI 의 `policy_profile` 색상 / 표시는 디자인 스펙 미합의 — 임시 정책 (read_safe=green / write_audited=orange / experimental=yellow) | UI 리뷰 시점에 합의 |
| 4 | 외부 클라이언트 (Claude Desktop 등) SDK 가 새 필드 인지하도록 업데이트 | 별 SDK 작업 |

---

## 10. 운영자 후속 (final review by 단계)

1. ✅ **운영자 macOS 실 pytest** — 본 FG 신규 27 + 누적 MCP 240 + 전체 3522 통과 (2026-04-28).
2. ⏳ **Admin UI UI 리뷰** — `policy_profile` 색상 / `default ★` / `stream ⤓` 시각 정책 합의.
3. ⏳ **외부 클라이언트 호환 모니터링** — 새 필드 출현으로 strict schema validator 가 실패하는 클라이언트 추적.
4. ⏳ **Codex 정적 리뷰** — S3 전체 종료 시점 일괄.

final review by:
- Codex (S3 종결 시점)
- @최철균 (P1 — 신규 운영자 endpoint + ScopeProfile 자동 등록 정책)

---

## 11. 변경 이력

| 일자 | 변경 | 작성자 |
|---|---|---|
| 2026-04-28 | 초판 — 5 manifest 필드 + 헬퍼 + admin endpoint + Admin UI 동적 fetch + 27 회귀. R1~R6 영향 0. | Claude |
