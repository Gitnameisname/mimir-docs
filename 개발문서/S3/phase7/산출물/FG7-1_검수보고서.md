# FG 7-1 검수보고서 — Valkey 인프라 정비

**작성일**: 2026-05-18
**대상**: Task 7-1 (Valkey 인프라 + 네임스페이스 + 폐쇄망 fallback)
**검수자**: Claude (1차 셀프 점검). 별 reviewer (Codex) + @최철균 P1 승인 게이트 잔여.

---

## 1. Intent ↔ Diff 정합성

| Intent (Task 7-1 §1) | Diff | OK |
|---|---|---|
| `app.cache.namespace` 신설 — env-aware key/channel 합성 | `backend/app/cache/namespace.py` (신규) | ✅ |
| `app.cache.policy` 신설 — fail-open/closed 분류 | `backend/app/cache/policy.py` (신규) | ✅ |
| `app.cache.valkey` 비활성 모드 노출 | `backend/app/cache/valkey.py` 수정 — `is_valkey_disabled()` + `get_valkey_or_none()` | ✅ |
| Config: `valkey_disabled` / `valkey_namespace` / override 4종 | `backend/app/config.py` 수정 | ✅ |
| 단위 테스트 신규 ≥ 12 case | `tests/unit/cache/` 36 case | ✅ |
| 기존 회귀 녹색 | Phase 7 영역 187 / 187 ✅ | ✅ |

손대지 않을 영역 (`viewed_throttle.py`, `scope_profile_policy.py`, `response_cache.py`, `rate_limit.py`) 은 본 Task 에서 영향 없음.

---

## 2. R-I 절대 규칙 매핑

| 규칙 | 본 Task 충족 |
|---|---|
| **R-I1 폐쇄망 호환** | `is_valkey_disabled()` 가 `VALKEY_HOST=""` / `VALKEY_DISABLED=1` 모두 감지. 회귀: `TestIsValkeyDisabled` 6 case ✅ |
| **R-I2 fail-open vs closed** | `FailPolicy` enum + 5 feature 분류 + env override + 보수 default (FAIL_CLOSED). 회귀: `TestEnvOverride.test_both_overrides_fail_closed_wins` ✅ |
| **R-I3 pub/sub 권한** | `make_channel(org_id=...)` 가 tenant prefix 강제. 회귀: `TestMakeChannel.test_tenant_isolation` ✅. 실제 pub/sub 권한 (AUTH) 은 운영 인프라 측 — 본 Task 는 채널 명명 계약만 |
| **R-I4 키 namespace** | `make_key()` 가 `mimir:<env>:` prefix 강제 + invalid char sanitize. 회귀: `TestMakeKey.test_namespace_isolation_dev_vs_prod` ✅ |

---

## 3. 회귀 결과

| 범위 | 결과 |
|---|---|
| Phase 7 신규 테스트 (`tests/unit/cache/`) | 36 / 36 ✅ |
| Phase 7 영역 + 인접 회귀 (audit + scope_profile_policy + scope_profile_settings + repository + documents filter) | 187 / 187 ✅ |
| 전체 unit test suite | 2852 / 2860 (8건 사전-존재 실패 — `test_annotations_service_fg33` mention regex + `test_embedding_dim_check` alembic 미설치. Phase 7 무관) |

전체 단위 회귀 비교 시 Phase 7 변경 전후의 unique 신규 fail 은 0 건.

---

## 4. 함수도서관 등록

`docs/함수도서관/backend.md` §1.11-fg71 신규 등록 (별 turn 에서 작성):
- `app.cache.namespace.make_key`
- `app.cache.namespace.make_channel`
- `app.cache.namespace.namespace_prefix`
- `app.cache.policy.policy_for` / `is_fail_open` / `is_fail_closed`
- `app.cache.valkey.is_valkey_disabled`
- `app.cache.valkey.get_valkey_or_none`

---

## 5. 위험 / 잔여

| # | 항목 | 상태 |
|---|---|---|
| R-1 | `get_valkey()` 호환성 — 본 Task 는 disabled 모드여도 instance 반환 유지 (운영 호출자 break 방지) | 보존 ✅ |
| R-2 | `mimir:` prefix 기존 키와 충돌 | 신규 호출자만 `make_key()` 사용 — 기존 `response_cache.py` 의 `mimir:doc:...` 등은 그대로. 점진 통일은 별 라운드 |
| 잔여-1 | 운영자가 실제 pub/sub AUTH 토큰 / 채널 ACL 을 인프라 측에서 설정 (R-I3 운영 영역) | 인프라 가이드에 명문화 (별 산출물) |
| 잔여-2 | 폐쇄망 회귀 ≥ 5 시나리오 chaos test | 별 산출물 (운영자 합의 후) |

---

## 6. 결론

Task 7-1 의 의도·범위·R-I 4개 규칙 모두 충족. 단위 회귀 36 신규 + 187 전체 영역 녹색.

**다음 단계**: Codex 또는 별 reviewer 의 2차 검토 + @최철균 P1 승인.
