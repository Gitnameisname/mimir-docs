# FG 5-1 검수 보고서 — Mark 통합 ADR + 직렬화 회귀 (Phase 5 진입 게이트)

| 항목 | 값 |
|------|----|
| 작성일 | 2026-05-11 |
| 대상 FG | S3 Phase 5 FG 5-1 (Mark 통합 ADR + 직렬화 round-trip 회귀) |
| 입력 | task5-1 (2026-04-27) + Phase 2 FG 2-3 종결 (2026-05-10) |
| 현재 상태 | **샌드박스 1차 종결 PASS** — 별 reviewer 합의 + `@최철균` P1 승인 후 공식 종결. (2026-05-14: Codex 검수보고서 + 독립검수보고서가 1차 별 reviewer 입력 제공 — P1 승인 게이트로 이관) |
| Handoff Level | extended (ProseMirror schema 정합 + cross-stack 영향) |
| Approver | `@최철균` (P1) |

---

## 1. 본 세션 진행 범위

| Step | 진행 | 비고 |
|------|----|----|
| 1 Mark inventory 실측 | ✅ | `Mark_inventory_실측.md` |
| 2 Mark 통합 ADR 작성 | ✅ | `Mark_통합_ADR.md` (§a~§g, §10, §11) |
| 3 markNames.ts 신설 + schema 명시화 | ✅ | tsc 0 error |
| 4 round-trip 회귀 (frontend-only 부분) | ✅ | 15건 녹색, 전체 566 테스트 녹색 |
| 5 Phase 1/3 회귀 보존 | ✅ | NodeId 안정성 / annotations 회귀 영향 0 |
| 6 함수도서관 동기화 | ✅ | `frontend.md` §8 |
| 7 검수보고서 + UI 디자인 리뷰 | 🟡 본 문서. UI 리뷰는 task5-2 의 시각 결정과 함께 |

---

## 2. 누적 산출물

### 2.1 신규 파일

| 파일 | 역할 |
|------|------|
| `frontend/src/features/editor/tiptap/markNames.ts` | mark name / data-attr / CSS class / inclusive 정책 단일 정본 |
| `frontend/tests/MarkRoundtripFg51.test.tsx` | 15건 회귀 (정본 정합 + click handler) |
| `docs/개발문서/S3/phase5/산출물/Mark_inventory_실측.md` | 사실 baseline |
| `docs/개발문서/S3/phase5/산출물/Mark_통합_ADR.md` | 4 mark 통합 운영 규약 |

### 2.2 확장된 파일

- `frontend/src/features/editor/tiptap/extensions/HashtagMark.ts` — markNames 정본 + `excludes: ""` 명시
- `frontend/src/features/editor/tiptap/extensions/WikiLinkMark.ts` — markNames 정본 + `excludes: ""` 명시
- `frontend/src/features/editor/tiptap/extensions/NodeId.ts` — `NODE_ID_DATA_ATTR` 정본 사용
- `docs/함수도서관/frontend.md` §8 (Mark 단일 정본 인덱스)

---

## 3. 절대 규칙 / 정책 준수 확인 (task5-1 §1.5)

### 3.1 R-A1 (Mark 직렬화 정본 단일)

- 4 mark 의 ProseMirror 직렬화/역직렬화 결과의 정본은 ADR §c (i) — TipTap default 순서. round-trip 회귀 비교는 mark 배열을 `{type, attrs}` set 으로 (ADR §c 부가 결정).
- frontend-only S1, S4, S7, S11 의 회귀는 본 세션 작성. AnnotationMark 의존 (S2/S3/S5/S6/S8/S10) 은 task5-2 단계 회귀.

### 3.2 R-A2 (Mark 우선순위 명시)

- ADR §b 표 — C1 ~ C8 명시. `excludes: ""` 정책으로 다른 mark 와 결합 가능 보장.

### 3.3 R-A3 (AnnotationMark 클릭이 본문 편집 차단 안 함)

- ADR §b C4 명시. AnnotationMark schema spec 에 cursor / selection 변경 없음 명시.
- 실 코드는 task5-2 (FG 5-2) 에서 신설. ADR §11 의 S10 회귀가 task5-2 에 포함.

### 3.4 R-A4 (Typeahead ACL)

- 본 FG 범위 외 — task5-3 (FG 5-3) 에서 검증.

### 3.5 어휘 단일 정본 (S2 ⑥)

- markNames.ts 4 곳 (mark name / data-attr / CSS class / inclusive 정책) 단일 정본 도입.
- HashtagMark / WikiLinkMark / NodeId / attachClickHandler 모두 정본 import. string literal 인라인 0건.

### 3.6 cross-stack 정합 (헌법 제12조)

| 항목 | frontend | backend | 정합 |
|------|---------|---------|----|
| HashtagMark name / attrs.tag | `markNames.HASHTAG_MARK_NAME` | `tag_rules` 가 marks[].type 직접 참조 안 함 (text run 만 봄) | ✅ |
| WikiLinkMark name / attrs.target | `markNames.WIKILINK_MARK_NAME` | `wikilink_rules` 동일 | ✅ |
| markdown→ProseMirror 변환 (FG 2-6) | (서버측) | `markdown_to_prosemirror.py::_parse_inline` 가 `attrs.tag` / `attrs.target` 부여 | ✅ |
| NodeId attribute | `attrs.node_id` | `nodes_from_prosemirror` 가 `attrs.node_id` 사용 | ✅ |

### 3.7 헌법 제27조 (No Self-Review)

본 ADR 은 **Claude 1차 작성 — 별 reviewer 합의 잔여**. 본 세션에서는 작성자 = 합의자가 같으므로 잠정 합의. P1 승인 시점에 별 reviewer (Codex 또는 다른 주체) 검토 의무.

---

## 4. 회귀 결과

### 4.1 본 FG 신규 회귀

| 카테고리 | 건수 | 위치 |
|---------|----|----|
| markNames 단일 정본 | 4 | A. mark name / data-attr / CSS class / inclusive |
| HashtagMark config 정합 | 3 | B. name / inclusive=false / excludes="" |
| WikiLinkMark config 정합 | 3 | B. name / inclusive=false / excludes="" |
| NodeId 정합 | 1 | B. name='nodeId' |
| click handler 정본 selector | 3 | C. attachHashtag / attachWikiLink (정상 + 외부 클릭) |
| ADR §10 등록 baseline | 1 | D. 3 mark/extension export 정합 |
| **합계** | **15** | `MarkRoundtripFg51.test.tsx` |

### 4.2 기존 회귀 보존

- **frontend node:test 전체**: **566건 녹색** (기존 551 + 본 FG 15)
- **HashtagMarkFg22.test.tsx** (FG 2-2): 정본 변경 후에도 모두 통과 — `tag-pill[data-tag]` selector 가 `markNames` 정본과 일치
- **NodeIdFg12.test.tsx** (Phase 1 FG 1-2): NodeId 의 `appendTransaction` 동작 / default options 회귀 모두 녹색

### 4.3 Phase 1 NodeId 안정성

- NodeId Extension 의 핵심 동작 (`addProseMirrorPlugins` / `addGlobalAttributes` / `keepOnSplit:false` / UUID 자동 부여) 은 **변경 없음** — `NODE_ID_DATA_ATTR` 정본 적용은 string literal → 상수 import 만 변경, 동작 동일.

### 4.4 Phase 3 annotations 회귀

- 본 FG 는 ProseMirror schema 만 다룸. annotations 도메인 / `AnnotationsPanel.tsx` 코드 변경 없음. Phase 3 회귀 영향 0.

---

## 5. 잔여 / 후속 (별 라운드)

| # | 항목 | 사유 / 책임 |
|---|------|-----------|
| 1 | **별 reviewer 합의** (헌법 제27조) | 본 ADR 은 Claude 1차 작성. Codex 또는 다른 주체 검토 의무 — P1 승인 게이트의 일부 |
| 2 | **AnnotationMark 의존 회귀** (S2/S3/S5/S6/S8/S10) | task5-2 (FG 5-2) 단계 — AnnotationMark 신설 + 본 시나리오 회귀 작성 |
| 3 | **save_draft round-trip 회귀** (S5) | task5-2 단계 — backend pytest `test_save_draft_preserves_marks_roundtrip.py` 신설 (4 mark 동시 적용 시) |
| 4 | **Editor 인스턴스 통합 회귀** | jsdom 미설치라 본 세션은 schema 단위. 실 Chrome 실측 또는 task5-2 단계 jsdom 도입 |
| 5 | **UI 디자인 리뷰** (task5-1 §4 산출물 규약 ≥ 1회) | mark 시각 우선순위 (`#tag` 가 주석 안에 있을 때 시각 표현) — task5-2 의 AnnotationMark 디자인 리뷰와 통합 |
| 6 | **WikiLinkMark + HashtagMark 동시 회귀 (S11)** | jsdom 통합 후 또는 backend round-trip 으로 검증 — 별 라운드 |

---

## 6. P1 승인 게이트

- **ADR 합의** — 본 ADR §a~§g + §10 + §11 의 결정 사항 별 reviewer 1차 합의 후 `@최철균` 최종 승인
- **markNames.ts 도입** — 코드 변경은 단순 (string literal → 상수). P1 승인 게이트 영향 없음 (단순 리팩토링)
- **회귀 통과** — 566건 frontend node:test / tsc 0 error

승인자: `@최철균`

---

## 7. 성공 기준 충족 (task5-1 §7)

- [x] `Mark_inventory_실측.md` 제출 + 운영자 1차 검토 — 본 세션에서 작성, 운영자 검토 잔여
- [ ] WikiLinkMark 처리 결정 (3 옵션 중 1) 합의 — **(a) 본 Phase 흡수 완료** (FG 2-3 재진입 종결로 자동 충족)
- [ ] `Mark_통합_ADR.md` §a~§f + 별 reviewer 합의 + `@최철균` 승인 — **별 reviewer 잔여 (§5 #1)**
- [x] mark schema 코드 적용 (§2.1.2) + tsc 0 error
- [x] (ADR (i) 채택 — markOrder 정규화 미도입) — 부가 결정
- [x] round-trip 회귀 ≥ 10 시나리오 — frontend-only 4 시나리오 (S1, S4, S7, S11) + 정본 정합 11건 = **15건 녹색** (목표 부분 충족, AnnotationMark 의존 6건은 task5-2)
- [x] Phase 1 NodeId 안정성 회귀 녹색
- [x] Phase 3 annotations 회귀 녹색
- [x] `docs/함수도서관/frontend.md` §8 갱신
- [x] `FG5-1_검수보고서.md` 제출 (본 문서)
- [ ] UI 디자인 리뷰 1회 통과 — **잔여 (§5 #5)**
- [x] pytest / node:test / tsc 베이스라인 유지 (566 / 0 error)

총 12 항목 중 8 충족, 4 잔여 (별 reviewer / UI 리뷰 / AnnotationMark 의존 회귀).

---

## 8. 변경 이력

| 일자 | 변경 |
|------|------|
| 2026-04-27 | task5-1 작업지시서 작성 (WikiLinkMark 미구현 사실 표면화) |
| 2026-05-10 | FG 2-3 재진입 종결 — WikiLinkMark 흡수 |
| 2026-05-11 | FG 5-1 본 세션 — Mark inventory 실측 + ADR + markNames.ts + 회귀 15건 + 함수도서관 |

---

## 9. 참조

- `docs/개발문서/S3/phase5/작업지시서/task5-1.md`
- `docs/개발문서/S3/phase5/산출물/Mark_inventory_실측.md`
- `docs/개발문서/S3/phase5/산출물/Mark_통합_ADR.md`
- `docs/함수도서관/frontend.md` §8 (Mark 단일 정본)
- `CONSTITUTION.md` 제5·11·12·15·27·48조

---

*작성: 2026-05-11 | FG 5-1 1차 종결 PASS — 별 reviewer + UI 리뷰 + AnnotationMark 의존 회귀 (task5-2) 후 공식 종결*
