# FG 5-3 검수 보고서 — 멘션 typeahead 자동완성 + 사용자 검색 API

| 항목 | 값 |
|------|----|
| 작성일 | 2026-05-11 |
| 대상 FG | S3 Phase 5 FG 5-3 |
| 입력 | task5-3 |
| 현재 상태 | **샌드박스 1차 종결 PASS** — TipTap suggestion 통합 / AnnotationsPanel 통합 / 통합 회귀는 별 라운드 |
| Approver | `@최철균` (P1) |

---

## 1. 본 세션 진행 범위

| Step | 진행 | 비고 |
|------|----|----|
| 1 Pre-flight | ✅ | mention 영속화 (C) plain text 채택, viewer scope = `user_org_roles` JOIN |
| 2 backend repository | ✅ | `search_by_display_name_in_orgs` — keyword-only viewer_user_id |
| 3 backend 라우터 + rate limit | ✅ | `GET /api/v1/users` + 60/min + audit q 미저장 |
| 4 회귀 9건 | ✅ | SQL 정합 5 + 응답 모델 2 + R-A4 격리 2 |
| 5 frontend lib + hook + popup | ✅ | tsc 0 error |
| 6 함수도서관 갱신 | ✅ | backend.md §8 + frontend.md §9 |
| 7 검수 / 보안 보고서 | ✅ (본 문서) — UI 디자인 리뷰는 task5-4 통합 |

## 2. 누적 산출물

### 2.1 백엔드 신규

| 파일 | 역할 |
|------|------|
| `backend/app/schemas/user_search.py` | `UserSearchItem` / `UserSearchResponse` (email/role/status 미정의 — R-A4 모델 강제) |
| `backend/app/api/v1/users_search.py` | `GET /api/v1/users` 라우터 + slowapi rate limit |
| `backend/tests/unit/test_user_search_fg53.py` | 9건 회귀 |

### 2.2 백엔드 확장

- `backend/app/repositories/users_repository.py` — `search_by_display_name_in_orgs` 메서드
- `backend/app/api/v1/router.py` — `/users` prefix include (admin `/admin/users` 와 path 충돌 없음)

### 2.3 프런트엔드 신규

| 파일 | 역할 |
|------|------|
| `frontend/src/lib/api/users_search.ts` | `usersSearchApi.search` |
| `frontend/src/features/documents/hooks/useUserSearch.ts` | React Query + 150ms debounce |
| `frontend/src/features/documents/MentionPopup.tsx` | popup + 키보드 네비 + a11y |

## 3. 절대 규칙 / 정책 준수 확인 (task5-3 §1.2 / §3)

### 3.1 R-A4 — Typeahead ACL 누설 차단

- **viewer_user_id 가 ActorContext 에서만 추출** — 라우터 코드 명시 (`viewer_user_id = actor.actor_id`). query/body 주입 차단.
- **`user_org_roles` JOIN** — viewer 와 같은 org 의 사용자만 SQL 차원에서 격리.
- **응답 모델 강제** — `UserSearchItem` 이 `{user_id, display_name}` 필드만 정의. email/role/status 등 누설 가능성 0 (Pydantic 직렬화 시 자동 제거).
- **단위 회귀**:
  - `test_sql_includes_user_org_roles_join_and_viewer_self_exclude` — JOIN 정합
  - `test_sql_params_include_viewer_id_twice` — viewer_user_id 가 SQL 파라미터로 두 번 (JOIN + 본인 제외)
  - `test_response_only_contains_user_id_and_display_name` — 키 정확성
  - `test_response_model_excludes_email_role_status` — Pydantic 보장
  - `test_other_org_user_not_returned` — multi-org 격리
  - `test_viewer_user_id_must_be_keyword_only` — positional 호출 시 TypeError

### 3.2 SQL Injection 방어

- psycopg2 placeholder (`%s`) — 파라미터 바인딩으로 차단
- ILIKE wildcard escape (`%` `_` `\`) — 추가 방어. 회귀 `test_wildcards_in_query_are_escaped` 검증.

### 3.3 PII 보호 (제24조)

- audit log 가 `q` prefix 자체를 저장하지 않음 — `q_length` + `result_count` 만 (`logger.info` 명시)
- 응답에 raw email / phone 등 PII 없음

### 3.4 Rate Limit

- backend slowapi `60/minute` per user (`_USER_SEARCH_LIMIT`)
- frontend `useUserSearch` 의 150ms debounce — 사용자 빠른 입력 시 호출 횟수 자체를 줄임 (정상 트래픽 보호)

### 3.5 Pre-flight 결정 정합

- **mention 영속화 (C)** plain text — Phase 3 FG 3-3 의 `mentioned_user_ids` 컬럼 재사용. 본문에는 `@display_name` text 만 (mark 별도 신설 안 함)
- **viewer scope** = `user_org_roles` JOIN — multi-org user 자연 지원

## 4. 회귀 결과

### 4.1 본 FG pytest

`backend/tests/unit/test_user_search_fg53.py` — **9건 녹색**

| 카테고리 | 건수 |
|---------|----|
| Repository SQL 정합 | 5 |
| Schema 응답 모델 | 2 |
| R-A4 multi-org 격리 | 2 |

### 4.2 frontend tsc

본 FG 신규 파일 (lib/api/users_search.ts + useUserSearch + MentionPopup) **tsc 0 error**.

### 4.3 누적 회귀

- backend pytest 본 FG 9건 — 별 fixture 영향 없음
- frontend node:test 회귀 영향 0 (본 FG 는 신규 파일만, 기존 변경 없음)

## 5. 잔여 (별 라운드)

| # | 항목 | 사유 |
|---|------|----|
| 1 | **TipTap suggestion 통합** | 본문 에디터 안 `@` 입력 시 popup — `@tiptap/suggestion` 의존성 추가 (P1 + 폐쇄망 미러). |
| 2 | **AnnotationsPanel textarea 에 MentionPopup 마운트** | `@<prefix>` 패턴 감지 + popup 표시 + 선택 시 textarea 본문 + mentioned_user_ids 통합 |
| 3 | **frontend node:test ≥ 6건** | popup keyboard nav / debounce / R-A4 응답 keys / loading / empty / hover |
| 4 | **백엔드 통합 테스트 (TestClient + DB fixture)** | 인증 401 / 2 organization × 5 prefix R-A4 / rate limit 429 / SQL injection 페이로드 5+ |
| 5 | **timing attack 정량 측정** | 같은 query 길이에서 결과 0건 vs 결과 N건 응답 시간 차이 < 5% |
| 6 | **UI 디자인 리뷰 ≥ 1회** | popup 4 viewport (desktop / narrow / tablet / mobile) — task5-4 사이드바 리뷰와 통합 권장 |
| 7 | **한국어 username 정책 (FG 5-5)** | 본 FG 외 — 한국어 username 합의 부재 시 영문 username 만 |

## 6. P1 승인 게이트

- API 표면 추가: `GET /api/v1/users` (1 endpoint)
- DB 변경 없음 (기존 `users` + `user_org_roles` 사용)
- 의존성 변경 없음 (slowapi 는 기존)
- 승인자: `@최철균`

## 7. 성공 기준 충족 (task5-3 §7)

- [x] Pre-flight 보고서 + mention 영속화 (A/B/C) 결정 + 운영자 합의 — 본 검수보고서에 통합
- [x] `/api/v1/users` 엔드포인트 신설
- [x] viewer organization scope 강제 (`viewer_user_id` keyword-only required)
- [x] **R-A4 회귀** — 단위 9건 (multi-org 매트릭스 통합 회귀는 잔여 #4)
- [x] MentionSuggestion / MentionPopup — popup 컴포넌트 + 키보드 네비
- [x] debounce 150ms 동작
- [x] 키보드 네비 (↑/↓/Enter/Esc/Tab)
- [ ] annotation submit 통합 — **잔여 #2**
- [x] rate limit 60/min (코드 적용 — 통합 회귀는 잔여 #4)
- [x] SQL injection / wildcard 회귀
- [x] audit log q 미저장
- [x] backend pytest ≥ 9 (단위)
- [ ] frontend node:test ≥ 6 — **잔여 #3**
- [x] tsc 0 error
- [x] `docs/함수도서관/backend.md` §8 + `frontend.md` §9 갱신
- [ ] UI 디자인 리뷰 — **잔여 #6**

총 16 항목 중 12 충족, 4 잔여.

## 8. 변경 이력

| 일자 | 변경 |
|------|------|
| 2026-04-27 | task5-3 작업지시서 작성 |
| 2026-05-11 | FG 5-3 본 세션 — Pre-flight + repository + router + 회귀 9 + frontend lib/hook/popup + 함수도서관 |

## 9. 참조

- `docs/개발문서/S3/phase5/작업지시서/task5-3.md`
- `docs/함수도서관/backend.md` §8
- `docs/함수도서관/frontend.md` §9
- `backend/app/repositories/users_repository.py::search_by_display_name_in_orgs`
- `backend/app/api/v1/users_search.py`
- `CONSTITUTION.md` 제5·11·12·17·19·20·24·45조

---

*작성: 2026-05-11 | FG 5-3 1차 종결 PASS — TipTap suggestion 통합 / 통합 회귀 / UI 리뷰 잔여*
