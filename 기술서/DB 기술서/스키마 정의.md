# DB 스키마 정의

**데이터베이스**: PostgreSQL 16 + pgvector  
**스키마**: public (기본)

---

## 테이블 목록

| 테이블 | 설명 | Phase |
|--------|------|-------|
| `documents` | 문서 기본 정보 | 3 |
| `versions` | 문서 버전 | 3→4 |
| `nodes` | 버전 내 콘텐츠 노드 | 3 |
| `audit_events` | API 감사 이벤트 | 4 |
| `idempotency_records` | 멱등성 키 관리 | 3 |
| `review_actions` | 검토/승인/반려 액션 | 5 |
| `workflow_history` | 워크플로 상태 전이 이력 | 5 |
| `change_logs` | 문서 변경 사유 기록 | 5 |
| `organizations` | 조직 | 7 |
| `roles` | 역할 정의 | 7 |
| `users` | 사용자 | 7 |
| `user_org_roles` | 사용자-조직-역할 매핑 | 7 |
| `document_types` | 문서 유형 및 플러그인 설정 | 7 |
| `background_jobs` | 비동기 백그라운드 작업 | 7 |
| `api_keys` | API 키 관리 | 7 |
| `document_chunks` | 벡터 임베딩 청크 | 10 |
| `embedding_token_usage` | 임베딩 API 토큰 사용량 | 10 |

**뷰:**

| 뷰 | 설명 |
|----|------|
| `search_index_stats` | FTS 인덱싱 현황 집계 |

---

## documents

문서 플랫폼의 최상위 엔티티.

```sql
CREATE TABLE documents (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    title       VARCHAR(500) NOT NULL,
    document_type VARCHAR(100) NOT NULL,        -- FK 없음, DocumentType은 플러그인으로 관리
    status      VARCHAR(50) NOT NULL DEFAULT 'draft',
    metadata    JSONB NOT NULL DEFAULT '{}',    -- 타입별 커스텀 필드
    summary     TEXT,
    created_by  VARCHAR(255),
    updated_by  VARCHAR(255),
    created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    -- Phase 4 추가
    current_draft_version_id     UUID REFERENCES versions(id) ON DELETE SET NULL,
    current_published_version_id UUID REFERENCES versions(id) ON DELETE SET NULL,
    -- Phase 8 추가
    search_vector tsvector        -- FTS 인덱스 컬럼 (트리거로 자동 갱신)
);
```

**인덱스:**
- `idx_documents_status` — status 필터링
- `idx_documents_document_type` — 유형 필터링
- `idx_documents_created_at DESC` — 기본 정렬
- `idx_documents_updated_at DESC` — 수정 시간 정렬
- `idx_documents_title` — 제목 정렬/필터
- `idx_documents_search_vector (GIN)` — FTS

**status 값:** `draft`, `published`, `archived`, `deprecated`

---

## versions

문서의 버전 단위 스냅샷.

```sql
CREATE TABLE versions (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    document_id     UUID NOT NULL REFERENCES documents(id) ON DELETE CASCADE,
    version_number  INTEGER NOT NULL,
    label           VARCHAR(200),
    status          VARCHAR(50) NOT NULL DEFAULT 'draft',
    change_summary  TEXT,
    source          VARCHAR(50) NOT NULL DEFAULT 'manual',  -- manual | system | import
    metadata        JSONB NOT NULL DEFAULT '{}',
    created_by      VARCHAR(255),
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    -- Phase 4 추가
    parent_version_id          UUID REFERENCES versions(id) ON DELETE SET NULL,
    restored_from_version_id   UUID REFERENCES versions(id) ON DELETE SET NULL,
    title_snapshot             VARCHAR(500),      -- publish 시 문서 제목 복사
    summary_snapshot           TEXT,
    metadata_snapshot          JSONB,
    content_snapshot           JSONB,             -- 전체 노드 트리 JSON
    published_by               VARCHAR(255),
    published_at               TIMESTAMPTZ,
    -- Phase 5 추가
    workflow_status            VARCHAR(50),       -- draft | in_review | approved | published | rejected | archived
    -- Phase 8 추가
    search_vector              tsvector,
    UNIQUE (document_id, version_number)
);
```

**인덱스:**
- `idx_versions_document_id`
- `idx_versions_created_at DESC`
- `idx_versions_version_number` on `(document_id, version_number DESC)`
- `idx_versions_status` on `(document_id, status)`
- `idx_versions_search_vector (GIN)`

---

## nodes

버전 내 콘텐츠의 최소 단위. 계층형 트리 구조.

```sql
CREATE TABLE nodes (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    version_id  UUID NOT NULL REFERENCES versions(id) ON DELETE CASCADE,
    parent_id   UUID REFERENCES nodes(id) ON DELETE SET NULL,
    node_type   VARCHAR(100) NOT NULL DEFAULT 'paragraph',
    order_index INTEGER NOT NULL DEFAULT 0,
    title       VARCHAR(500),
    content     TEXT,
    metadata    JSONB NOT NULL DEFAULT '{}',
    created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    -- Phase 8 추가
    search_vector tsvector
);
```

**node_type 값:** `paragraph`, `heading`, `list_item`, `table`, `image`, `code_block`, `section`

**인덱스:**
- `idx_nodes_version_id`
- `idx_nodes_parent_id`
- `idx_nodes_order` on `(version_id, order_index ASC)`
- `idx_nodes_search_vector (GIN)`

---

## audit_events

모든 쓰기 API 호출의 감사 로그.

```sql
CREATE TABLE audit_events (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    event_type      VARCHAR(100) NOT NULL,        -- document.created, version.published 등
    occurred_at     TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    actor_user_id   VARCHAR(255),
    actor_role      VARCHAR(100),
    document_id     UUID,
    version_id      UUID,
    target_version_id UUID,
    previous_state  VARCHAR(100),
    new_state       VARCHAR(100),
    action_result   VARCHAR(50) NOT NULL DEFAULT 'success',  -- success | failure
    reason          VARCHAR(500),
    request_id      VARCHAR(255)
);
```

**보존 정책:** 90일 후 자동 삭제 (`scripts/data_retention.py`)

---

## idempotency_records

POST 쓰기 요청의 멱등성 키 관리.

```sql
CREATE TABLE idempotency_records (
    id                   UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    idempotency_key      VARCHAR(512) NOT NULL,
    actor_id             VARCHAR(255),
    resource_action      VARCHAR(200) NOT NULL,
    request_fingerprint  VARCHAR(128) NOT NULL,
    status               VARCHAR(50) NOT NULL DEFAULT 'in_progress',
    response_status_code INTEGER,
    response_body        JSONB,
    resource_id          VARCHAR(255),
    request_id           VARCHAR(255),
    trace_id             VARCHAR(255),
    tenant_id            VARCHAR(255),
    created_at           TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at           TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    expires_at           TIMESTAMPTZ,
    UNIQUE (idempotency_key, actor_id, resource_action)
);
```

---

## review_actions

검토/승인/반려 액션 상세 기록.

```sql
CREATE TABLE review_actions (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    document_id UUID NOT NULL REFERENCES documents(id) ON DELETE CASCADE,
    version_id  UUID NOT NULL REFERENCES versions(id) ON DELETE CASCADE,
    action_type VARCHAR(50) NOT NULL,    -- submit | approve | reject | publish | archive
    from_status VARCHAR(50) NOT NULL,
    to_status   VARCHAR(50) NOT NULL,
    actor_id    VARCHAR(255),
    actor_role  VARCHAR(100),
    comment     TEXT,
    reason      TEXT,
    metadata    JSONB NOT NULL DEFAULT '{}',
    created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

---

## workflow_history

워크플로 상태 전이 이력.

```sql
CREATE TABLE workflow_history (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    document_id UUID NOT NULL REFERENCES documents(id) ON DELETE CASCADE,
    version_id  UUID NOT NULL REFERENCES versions(id) ON DELETE CASCADE,
    from_status VARCHAR(50) NOT NULL,
    to_status   VARCHAR(50) NOT NULL,
    action      VARCHAR(50) NOT NULL,
    actor_id    VARCHAR(255),
    actor_role  VARCHAR(100),
    comment     TEXT,
    reason      TEXT,
    created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

---

## organizations

조직 단위.

```sql
CREATE TABLE organizations (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name        VARCHAR(255) NOT NULL,
    description TEXT,
    status      VARCHAR(50) NOT NULL DEFAULT 'ACTIVE',
    created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

---

## users

플랫폼 사용자.

```sql
CREATE TABLE users (
    id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email        VARCHAR(255) NOT NULL UNIQUE,
    display_name VARCHAR(255) NOT NULL,
    status       VARCHAR(50) NOT NULL DEFAULT 'ACTIVE',  -- ACTIVE | INACTIVE | SUSPENDED
    role_name    VARCHAR(100) NOT NULL DEFAULT 'VIEWER',  -- 플랫폼 기본 역할
    last_login_at TIMESTAMPTZ,
    created_at   TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at   TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

---

## user_org_roles

사용자-조직-역할 N:M 매핑.

```sql
CREATE TABLE user_org_roles (
    id        UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id   UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    org_id    UUID NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,
    role_name VARCHAR(100) NOT NULL,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE (user_id, org_id, role_name)
);
```

---

## roles

시스템 정의 역할.

```sql
CREATE TABLE roles (
    id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name        VARCHAR(100) NOT NULL UNIQUE,
    description TEXT,
    is_system   BOOLEAN NOT NULL DEFAULT FALSE,
    created_at  TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
-- 기본 시스템 역할: SUPER_ADMIN, ORG_ADMIN, AUTHOR, REVIEWER, APPROVER, VIEWER
```

---

## document_types

문서 유형 정의 및 플러그인 설정.

```sql
CREATE TABLE document_types (
    type_code    VARCHAR(100) PRIMARY KEY,
    display_name VARCHAR(255) NOT NULL,
    description  TEXT,
    status       VARCHAR(50) NOT NULL DEFAULT 'ACTIVE',
    schema_fields JSONB NOT NULL DEFAULT '[]',  -- 타입별 metadata 스키마
    plugin_config JSONB NOT NULL DEFAULT '{}',  -- 플러그인 오버라이드 설정
    created_at   TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at   TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
-- 내장 타입: POLICY, MANUAL, REPORT (기본 시드 데이터)
```

---

## background_jobs

비동기 백그라운드 작업 상태 관리.

```sql
CREATE TABLE background_jobs (
    id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    job_type      VARCHAR(100) NOT NULL,  -- vectorize_document, ...
    resource_type VARCHAR(100),
    resource_id   VARCHAR(255),
    resource_name VARCHAR(500),
    status        VARCHAR(50) NOT NULL DEFAULT 'PENDING',  -- PENDING | RUNNING | COMPLETED | FAILED
    progress      INTEGER DEFAULT 0,       -- 0~100
    requester_id  VARCHAR(255),
    requester_name VARCHAR(255),
    error_code    VARCHAR(100),
    error_message TEXT,
    metadata      JSONB NOT NULL DEFAULT '{}',
    started_at    TIMESTAMPTZ,
    ended_at      TIMESTAMPTZ,
    created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

**보존 정책:** 30일 후 자동 삭제

---

## api_keys

API 키 관리. 원문 키는 저장하지 않고 해시만 보관.

```sql
CREATE TABLE api_keys (
    id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name         VARCHAR(255) NOT NULL,
    description  TEXT,
    key_prefix   VARCHAR(20) NOT NULL,   -- Key 앞 8자리 (인덱스 조회용)
    key_hash     VARCHAR(255) NOT NULL,  -- SHA-256 해시
    scope        VARCHAR(100) NOT NULL DEFAULT 'READ_ONLY',
    status       VARCHAR(50) NOT NULL DEFAULT 'ACTIVE',  -- ACTIVE | REVOKED
    issuer_id    VARCHAR(255),
    issuer_name  VARCHAR(255),
    last_used_at TIMESTAMPTZ,
    last_used_ip VARCHAR(50),
    use_count    INTEGER NOT NULL DEFAULT 0,
    expires_at   TIMESTAMPTZ,
    revoked_reason TEXT,
    created_at   TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

---

## document_chunks

벡터 임베딩 청크. pgvector 확장 필요.

```sql
CREATE TABLE document_chunks (
    id               UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    document_id      UUID NOT NULL REFERENCES documents(id) ON DELETE CASCADE,
    version_id       UUID NOT NULL REFERENCES versions(id) ON DELETE CASCADE,
    node_id          UUID REFERENCES nodes(id) ON DELETE SET NULL,
    chunk_index      INTEGER NOT NULL,
    source_text      TEXT NOT NULL,
    embedding        vector(1536),       -- OpenAI text-embedding-3-small
    embedding_model  VARCHAR(100),
    token_count      INTEGER,
    node_path        TEXT[] NOT NULL DEFAULT '{}',  -- 인용용 계층 경로
    document_type    VARCHAR(100) NOT NULL,
    document_status  VARCHAR(50) NOT NULL,
    accessible_roles    TEXT[] NOT NULL DEFAULT '{}',  -- 권한 필터
    accessible_user_ids TEXT[] NOT NULL DEFAULT '{}',
    accessible_org_ids  TEXT[] NOT NULL DEFAULT '{}',
    is_public        BOOLEAN NOT NULL DEFAULT FALSE,
    is_current       BOOLEAN NOT NULL DEFAULT TRUE,  -- 최신 버전 청크 여부
    created_at       TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at       TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

**HNSW 벡터 인덱스:**
```sql
CREATE INDEX idx_chunks_embedding_hnsw
    ON document_chunks USING hnsw (embedding vector_cosine_ops)
    WITH (m = 16, ef_construction = 64)
    WHERE embedding IS NOT NULL AND is_current = TRUE;
```

---

## embedding_token_usage

임베딩 API 비용 추적.

```sql
CREATE TABLE embedding_token_usage (
    id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    job_id       UUID REFERENCES background_jobs(id) ON DELETE SET NULL,
    document_id  UUID REFERENCES documents(id) ON DELETE SET NULL,
    model        VARCHAR(100) NOT NULL,
    total_tokens INTEGER NOT NULL DEFAULT 0,
    chunk_count  INTEGER NOT NULL DEFAULT 0,
    created_at   TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

---

## search_index_stats (뷰)

FTS 인덱싱 현황 집계 뷰.

```sql
CREATE OR REPLACE VIEW search_index_stats AS
SELECT
    'documents' AS table_name,
    COUNT(*) AS total_rows,
    COUNT(*) FILTER (WHERE search_vector IS NOT NULL) AS indexed_rows,
    COUNT(*) FILTER (WHERE search_vector IS NULL) AS unindexed_rows
FROM documents
UNION ALL
SELECT 'versions', COUNT(*), ... FROM versions
UNION ALL
SELECT 'nodes', COUNT(*), ... FROM nodes;
```

---

## 데이터 보존 정책 요약

| 테이블 | 보존 기간 | 기준 컬럼 |
|--------|-----------|-----------|
| `audit_events` | 90일 | `occurred_at` |
| `document_versions` | 365일 (비활성) | `created_at` |
| `background_jobs` (COMPLETED/FAILED) | 30일 | `created_at` |
| `idempotency_records` | 24시간 (expires_at) | `expires_at` |

자동 정리: `scripts/data_retention.py` (주 1회 실행 권장)
